# Fifteen DOS programs under the box — 2026-09-16

**Taken on `919a621`** (`claude/dos-arena-home`), under MartyPC on
`os8088_xt_vga` and `os8088_xt_vga_144` — a 4.77 MHz 8088 with a VGA. The
reference column is **IBM DOS 3.30** booted off a real floppy under the same
emulator, same disk, same machine.

This is a MEASUREMENT and true of that tree only. A later tree wants a new
file, not an edit to this one.

The software is the owner's own collection and is not in this repository. What
is reproducible from here is the METHOD: `tools/os88intmon.py` for the
`INT 21h` traffic, `Marty.disk()` for the drive, and the EXE header's
`minalloc` for what a program is asking the loader for.

---

## The table

| program | windowed box | kern_dos | what decided it |
|---|---|---|---|
| ALDO | **runs** | — | |
| JOUST | **runs**, mode 13h | — | |
| ROCKFORD | **runs** — *"Do you have a joystick?"* | — | |
| NUM MUNCHERS | **runs** — *"Do you want instructions?"* | — | **31,301 `INT 21h` calls** in 120 guest seconds; the interrupt monitor is the wrong instrument for it |
| STAR TREK | **runs** | — | |
| **BOLOBALL** | **runs** (was `Exit code 002`) | — | §96.7.1.3 and §96.5.2 — two defects, both ours |
| **COMMANDER KEEN 2** | *"Out of memory!"* — correct | **runs**, title level | §96.9.2 — the allocator's ceiling ratcheted down |
| **TOM & JERRY** | *"Program too big to fit in memory"* — **correct** | **runs**, level select | `TJ.EXE` declares `minalloc` 27,990 paragraphs: **483 KB** against the windowed arena's 446 |
| **CHIP 'N DALE** | `00011: Program Internal Error.` — **correct** | **runs**, high scores | 376 KB taken, 56 KB left, 62 KB asked for — short by **three kilobytes** |
| BATTLE CHESS | — | **runs**, title screen | |
| TEST DRIVE 1 | — | **runs**, adapter menu | |
| TEST DRIVE III | — | **runs**, through the Lamborghini intro | the scene §96's own notes had it locking on |
| AGENT | **decompressing, slowly** | — | not a defect: 8,064-byte reads into successive segments, every one `AX=1F80 CF=0`. A 316 KB payload on a 4.77 MHz 8088 |
| BACKYARD | `SHELL.EXE` prints *"To launch The Playworld, type: PLAYWRLD"* and exits | same | not a defect: its own launcher is a `.BAT` that runs `SHELL` **with an argument**, and this box has no batch interpreter |
| LEMMINGS | not reached | — | harness, not the box: 67 files in one folder and the Disk window lists 32 |

**Twelve of the fifteen run.** Two of the three that do not are the program's
own doing and one is the harness's. Nothing in this set is left failing for a
reason belonging to this box.

**Six want more than the windowed arena and every refusal is correct** — the
arithmetic is in the rows above. That is the third radio arm earning its
keep rather than a limitation: it is the difference between nine programs and
twelve.

## The three defects it found, in the order they bit

1. **`AH=25h` destroyed `AX`** (§96.7.1.3). `.setvec` fetched the program's
   `DS` off the banked frame with `mov ax, [bp]`. Every compiled DOS program
   installs its 8087 emulator's vectors with `mov ax, 2534h / int 21h /
   inc ax / loop`, so the second lap ran with a fabricated `AH`: `41h` DELETE
   FILE, then `00h` TERMINATE. Nineteen calls and BOLOBALL was gone.
2. **Fourteen of DOS's own vectors were `0000:0000`** (§96.5.2). Not
   *unimplemented* — a jump to address zero, and the probe IS the call.
   BOLOBALL asks `int 2Ah AH=00h` three instructions after a version check we
   answer correctly.
3. **The allocator never coalesced adjacent free blocks** (§96.9.2). Keen 2
   was granted 483 KB and later refused 420 KB with 221 KB free above it in
   three pieces.

**None of the three is visible in our own trace.** Every call was answered
correctly; only the sequence was wrong, and only the register, the vector or
the chain carries it. That is docs/DOS-DEBUGGING.md's premise restated by three
more cases: what found all three was putting the same program in front of a
real IBM DOS and aligning on (function, call site).

## What the arena decides, and what it does not

The three whose arithmetic is worth writing out:

| | wants | windowed arena | kern_dos |
|---|---|---|---|
| TOM & JERRY | 483 KB at LOAD (`minalloc`) | 446 KB | 579 KB |
| COMMANDER KEEN 2 | grows to 420 KB at RUN | 446 KB | 579 KB |
| CHIP 'N DALE | 438 KB of allocations at RUN | 435 KB of chain | 579 KB |

Keen is the one to read twice. It fails in the windowed box for a real reason
and it failed in **kern_dos for a different one**, and the two look identical
from outside — the same message at the same call. The first theory, that
kern_dos was 11 KB short of DOS's 590, was **tested and refuted**: a
`KD_RAH_KEEP=0` build hands the program 588 KB and it fails at the *identical*
call. The quantity was never the problem.

## Chip 'n Dale — it looked like a hang three times and was never one

Worth writing down because every intermediate reading pointed the wrong way,
and each one was disproved by the next measurement rather than by argument.

1. **"It is slow."** No: `Marty.disk()` read `read_sectors = 834` identically
   across four samples thirty guest seconds apart. Agent, which really is slow,
   keeps climbing.
2. **"It is hung."** No: sampling `CS:IP` showed **134 million cycles in 25
   guest seconds** — the full 4.77 MHz — split between our `INT 21h` handler
   and the ROM's `INT 16h`. A live keyboard poll, 35,680 `AH=0Bh` calls. It
   was waiting to be allowed to speak, and the first keystroke got
   `00011: Program Internal Error.` and exit code 011.
3. **"It is a defect in us."** It diverges from IBM DOS 3.30 at exactly one
   call — 214 non-polling calls agree, site for site — and that call is
   `AH=48h BX=0FA2`, 62 KB, which DOS grants and we refuse. **The refusal is
   right.** The chain behind it:

       2520 M own=2542 size=0020   the environment
       2541 M own=2542 size=3BA5   238 KB
       60E7 M own=2542 size=0115     4 KB
       61FD M own=2542 size=0FA2    62 KB
       71A0 M own=2542 size=1000    64 KB
       81A1 M own=2542 size=0200     8 KB
       83A2 Z own=0000 size=0E3D    56 KB   FREE

   376 KB already taken, 56 KB left, 62 KB asked for. The whole chain is
   `2520`..`91DF` — **435 KB**, and the program wants 438. It is short by
   **three kilobytes**, and under kern_dos it reaches its high-score table.

The lesson is the one docs/DOS-DEBUGGING.md opens with, arrived at from the
other end: our own answer was CORRECT at every call including the last, so no
amount of reading our trace would have said anything. What separated *"we are
wrong"* from *"the machine is smaller"* was the chain — six lines of guest
memory — and nothing else in the run could have.

## Two things about the harness, for whoever runs this next

**`os8088_xt_vga_144` is new and the tree needed it.** Every VGA profile here
had 360 KB drives and every 720 KB/1.44 MB profile was a Hercules or a CGA, so
a program whose own documentation says `Req. EGA/VGA` and whose payload is
600 KB could be hosted on neither. The 40-cylinder trap
(docs/DOS-DEBUGGING.md, `reach`) is the other half: the 720 KB profiles here
are 40-cylinder, so a 720 KB image's top half answers an `int 13h` error that
our read window turns into end of file. 1.44 MB drives are 80-cylinder and
have neither problem.

**Do not sweep with the interrupt monitor.** It stops the guest on every
`INT 21h`, which is exactly right for diagnosis and hopeless for triage: Num
Munchers makes 31,301 of them. Boot, wait a fixed number of GUEST seconds,
screenshot, read `Marty.disk()` — the drive counters are what separate *slow*
from *hung*, and they cost nothing.
