# RUNCPM port plan — RunCPM 6.9 as an os8088 C package

The design record for SPEC.md §74, produced by `.claude/skills/port-to-os8088`'s scouting workflow (one source scout, three tree scouts, a planner, three adversarial reviewers, a reconciler) on 2026-08-17 and amended by the user's decisions below. `workflows/implement.js` reads this file one wave at a time. The reference source is https://github.com/MockbaTheBorg/RunCPM (MIT); it lives outside this repo and nothing from it is vendored (CONTRIBUTING.md §6).

## Summary

Port RunCPM 6.9 (CP/M 2.2 only) to os8088 as one C package, RUNCPM.O88 (+ RUNCPM.OVL if the measured size demands it), with the Z80 core and every Z80-RAM access hand-written in 8086 assembly inside the shim and everything else - _PatchCPM, BIOS, BDOS 0-40 + 230/231/248-254, the FCB/record layer, the console, the CCP glue - as C reimplemented from cpm.h/disk.h/console.h/globals.h/main.c. The structural change from RunCPM: NO worker task and NO blocking anywhere. The emulator runs on the UI task in bounded slices, re-entered through a new kernel wake event (OSAPI_WM_WAKE posts EVT_WAKE; the UI task dispatches W_ONWAKE WITHOUT the gfx lock; os88_onwake takes the lock only around the terminal flush), so file slots are always legal, the 1024-byte UI stack is the only stack, and a Z80 program waiting on a key costs zero CPU until os88_onkey pushes the key and kicks. Every blocking BDOS call (CONIN 1/6, BIOS CONIN, the line editor 10) is either 'retry the trap when a key exists' or a small state machine advanced by os88_onkey. The Z80 core's register plan, corrected from the draft: A/F in AL/AH (F in lahf layout with P/V and N fixups), BC in CX, DE in DX, HL in DI, PC in SI, Z80 SP in BP (ds: override on every stack access), BX = dispatch scratch (`xor bh,bh / mov bl,[si] / inc si / shl bx,1 / jmp [cs:bx+tab]`, no lodsb - it would clobber A), IX/IY/alt set/I/R/IFF in cs: statics, DS = the 64KB Z80 claim; DDCB/FDCB reuse the CB table through an operand pointer (documented r=6 forms only). The console is an 80x25 VT100-subset terminal model (RC_ROWS = 25, the reference geometry TE.COM is configured for) drawn damage-only through a glass shadow: gfx_scroll + one repainted row per line, a composed 1bpp band + one gfx_blit1 per changed row at every width (font_run only as the blit1-refused fallback - measured, PERFORMANCE.md Set 65); in a framed window a 640-px screen shows 79 of 80 columns (measured, §74.2 - the window is flush with the screen and has no left border) and CGA 17 of 25 rows, and the fullscreen latch (Decision 2) is the one mechanism that gives the full 80x25 on CGA. CP/M drives are folders A\0.. below the launch folder; the BDOS record model is whole-file-in-a-heap-claim with an 8-entry open-file table, a per-(drive,user) directory cache, write-back on F_CLOSE / warm boot; files above 65,535 bytes are refused (decided, not asked) and are not shipped on any geometry. The drive/user switch is NOT OSAPI_FILE_GOTO_Q as drafted (a package's next file call re-stands it in the instance folder via inst_vol_enter): it is a goto_q variant that also marks the instance, folded into the same wave-1 kernel work as the wake, with os88_file_goto as the fallback. The command processor is DRI's own CCP-DR.60K running on the Z80. Estimated Z80 speed is a number to MEASURE on the first core build (the draft's 0.25-0.35 MHz XT figure rested on an inner loop that could not be written); the banner prints the measured figure. Budget re-derived at cword's all-in ratio (5.9 bytes/line): 36-47KB image + ~11.5KB bss - at or past the 55,000 trigger, so the ovl_* candidates are named now and LESSONS 2's stub build happens before wave 2. Disk rules, the vm/386-runcpm machine and a debug .COM loader move to wave 2 so waves 2-4 can meet their own done_when; the CCP binary's provenance and the allapps fetch are the user's call (Decision 3).

## Decisions (the user's, 2026-08-17)

1. **Kernel change: yes, both** — add `OSAPI_WM_WAKE` / `OSAPI_WM_ONWAKE` (dispatched on the UI task WITHOUT the gfx lock; `CC_HAS_ONWAKE` in crt0; thunks) and the instance-marking variant of `OSAPI_FILE_GOTO_Q` (`os88_file_goto_q_mark`). Measured against 429 bytes left in the image rung; no budget constant is raised. The emulator has no worker.
2. **Fullscreen: yes** — thunk `os88_fullscreen` (SPEC.md §11.2's latch), bind **Alt+F** in both directions; SPEC.md §74.2 states the exception to §11.2.1 and why (a terminal owns F and Esc). The chord must be verified to reach the package with ascii 0 and a distinct scan code and not to be consumed by the kernel first.
3. **Disk content: fetch at build, ship all that fits** — nothing third-party is committed (CONTRIBUTING.md §6 rules the plan's 'commit the 2KB CCP' out); `tools/getruncpm.py` fetches RunCPM at a pinned commit (the `getstories.py` precedent), unpacks `DISK/A0.zip` and copies `CCP/CCP-DR.60K`; the runcpm disks and `apps-all.img` carry everything on the master disk that fits and is under 65,535 bytes, MBASIC and Z80ASM included; the three files above 65,535 bytes are never shipped and the disk's readme says so. `make allapps` acquires the fetch as a prerequisite (it already needs the C toolchain).
4. Intake: the user chose 'port it whole and grey what cannot carry'; the program name is inferred (RunCPM 6.9); the licence is MIT and the attribution goes into every file header carrying derived material, into the About box and into the PR body.

Everything else below was decided by the plan and stands unless a wave shows it wrong — and then this file and SPEC.md §74 are amended together.

## Authority table (surface → the RunCPM file that defines it)

- **boot banner: '  CP/M Emulator v6.9 by Marcelo Dantas', 'Built <pinned upstream commit date>' (stated deviation: __DATE__/__TIME__ would break byte-for-byte rebuilds), the dashes rule, 'CPU is 8086 native' (new CPU_IS - this core is not cpu1.h's 'Model 1'), '<n> T-states in <n> ms' (smaller magnitudes: a reduced burst, stated), 'Estimated Z80 clock speed: N.NN MHz' (stated deviation from cpu_mhz.h's integer %u, which would print '0 MHz' on the XT), 'BIOS at 0xfe00 - BDOS at 0xec00', 'BIOS/BDOS using interrupt handoff method', 'CCP CCP-DR.60K at 0xe400' (lowercase hex as printed: globals.h tohex() is x+87, console.h _puthex16), 'Unable to load CP/M CCP.' 'CPU halted.'; 'FILEBASE is ./' (main.c 96-99, every desktop build's #ifdef FILEBASE) is NOT printed - the base is the launch folder and a package has no path string to name it by** — RunCPM/main.c lines 66-105; RunCPM/cpu_mhz.h; RunCPM/cpu1.h:7 (CPU_IS); the four deviations pinned in the RunCPM SPEC section
- **CCPHEAD warm-boot header '\r\nRunCPM Version 6.9 (CCP-DR.60K) - CP/M 2.2\r\n', VERSION '6.9', VersionBCD 0x69, CCPname/VersionCCP/BatchFCB/CCPaddr per CCP_* block, TPASIZE 60, PAGESIZE/BIOSjmppage 0xFE00/BIOSpage 0xFF00/BDOSjmppage 0xEC00/BDOSpage 0xED00/DPBaddr/tmpFCB layout defines, BOOTONLY FALSE (AUTOEXEC.TXT on EVERY warm boot, from FILEBASE = the launch folder)** — RunCPM/globals.h lines 55-135, 240-270, 305; RunCPM/main.c 125-137
- **page zero, BIOS/BDOS jump pages, RST 08h/RST 10h stubs, DPB/DPH, block/extent constants (blockShift, numAllocBlocks, physicalExtentBytes, logicalExtentBytes 16*1024)** — RunCPM/cpm.h _PatchBIOS/_PatchCPM lines 158-296
- **BIOS entries BOOT..SECTRAN and their register answers (B_LIST 582 and B_AUXOUT 585 are EMPTY - no-ops here too)** — RunCPM/cpm.h _Bios lines 549-725
- **BDOS functions 0-40 and 230, 231, 248-254 (result plumbing HL -> A/B, DRV_ALLRESET, _CheckSUB), A_WRITE 4 / L_WRITE 5 -> A/0/PUN.TXT and A/0/LST.TXT, F_ATTRIB answers HL=0, and the C_READSTR line editor key map (^A ^B ^C ^E ^F ^G ^H/DEL ^J/^M ^K ^R ^U ^W ^X ^? and its help text, bell on refusal, printable 0x20-0x7E and ^Z)** — RunCPM/cpm.h _Bdos lines 738-2156, 795-818 (4/5), 1390 (F_ATTRIB), C_READSTR lines ~984-1170 (1035 DEL, 1120 ^?), F_SETMASK..F_CCPADDR lines 2038-2135; RunCPM/host.h F_BDOSCALL 231
- **console byte semantics: mask8bit 0x7F on every output byte, no translation of CR/LF/BS/BEL/TAB/ESC, _puthex8/16, _putdec** — RunCPM/console.h (133 lines)
- **BDOS disk error text '\r\nBdos Err on X: ' + 'R/O' | 'Select' | '\r\nCP/M ERR', wait for a key, warm boot** — RunCPM/disk.h _error lines 22-41
- **FCB (36 bytes) and directory entry (32 bytes) layouts; _FCBtoHostname/_HostnameToFCB name rules incl. NOSLASH; _mockupDirEntry extent/rc/al synthesis INCLUDING the multi-entry extent chain for files above one physical extent (one entry per extentsPerDirEntry, disk.h 229-247); _OpenFile/_CloseFile/_MakeFile/_SearchFirst/_SearchNext/_DeleteFile/_RenameFile/_ReadSeq/_WriteSeq/_ReadRand/_WriteRand/_GetFileSize/_SetRandom/_SetUser/_MakeDisk/_CheckSUB behaviours and return codes** — RunCPM/disk.h (894 lines); RunCPM/abstraction_posix.h typedef CPM_FCB/CPM_DIRENTRY, FOLDERCHAR, FILEBASE
- **drive/user folder layout A..P / 0..9,A..F, INFO.TXT, PUN.TXT/LST.TXT capture, AUTOEXEC.TXT, $$$.SUB reverse-record format** — RunCPM/disk.h, RunCPM/cpm.h A_WRITE/L_WRITE, RunCPM/ccp.h _ccp_readInput, RunCPM/globals.h BATCHA/AUTOEXEC
- **HALT: the shipping (non-DEBUG) build sets STATUS_EXIT silently - '::CPU HALTED::' is #ifdef DEBUG and is NOT printed here; this port toasts 'RunCPM: CPU halted' (an OS mechanism, stated as a deviation in SPEC) and self-closes** — RunCPM/cpu1.h case 0x76 lines 1687-1700 (DEBUG-guarded); RunCPM/main.c exit path
- **HostOS code (new value 0x08 for os8088, beside 0x00 Windows/0x01 Arduino/0x02 posix/0x03 DOS/0x04 Teensy/0x05 ESP32/0x06 STM32/0x07 Pico); the shipped INFO.COM maps only 0..7 and prints 'running on Unknown' for 0x08 - greyed as a fact** — RunCPM/abstraction_posix.h line 23, abstraction_windows.h line 19, abstraction_arduino.h lines 9-21; A0.zip INFO.Z80 lines 41-95
- **terminal geometry 80x25 ANSI/VT100 assumed; _clrscr = ESC[2J ESC[H; TE.COM's TE_CONF block at 0x127B says 25 rows x 80 cols** — RunCPM/abstraction_runvt.h _console_init/_clrscr; RunCPM/readme.md 'Limitations'; A0.zip TE.COM (TE_CONF)
- **CCP-DR.60K binary, its $$$.SUB FCB offset (+0x7AC), entry with C = DSKByte, load at BDOSjmppage-0x800** — RunCPM/CCP/CCP-DR.60K; RunCPM/globals.h CCP_DR block; RunCPM/main.c _RamLoad(CCPname, CCPaddr)
- **master disk contents (79 files, 875,602 bytes - the plan's 81 was a miscount, corrected in wave 2's review; the three above 65,535 bytes - Z80ASM.PDF 134,899, BDOS.ASM 67,301, ZCPR3.ASM 66,761 - are not shipped), 1STREAD.ME text, per-program credits, INFO.TXT 'Main RunCPM disk'** — RunCPM/DISK/A0.zip, RunCPM/DISK/1STREAD.ME
- **the 16x16 icon: a two-row window strip with its close box over a dark field carrying a large green `A>` - transcribed, not drawn** — RunCPM/RunCPM.ico (the only image in it; IDI_ICON1 in RunCPM/RunCPM.rc)
- **licence text and attribution ('Copyright (c) 2017 Mockba the Borg', file headers 'Copyright (c) 2016 - Marcelo Dantas')** — RunCPM/LICENSE; RunCPM/main.c, cpm.h headers
- **Z80 instruction semantics and flag results the assembly core must reproduce (the reference the ZEXDOC gate compares against)** — RunCPM/cpu1.h (4249 lines) - behaviour only, no line ports; A0.zip ZEXDOC.COM/ZEXALL.COM as the oracle
- **keyboard: '^? = Ctrl+-  and DEL = Ctrl+Backspace on this keyboard' (the help line advertises both and int 16h delivers chr 31 and 0x7F only that way); Ctrl-H/I/M fold into BS/Tab/CR (identical bytes to what CP/M expects)** — RunCPM/cpm.h 1035, 1120; kernel/keyboard scan-code path (verified by screendump in wave 3)

## Scope

### Ships

- Z80 core in 8086 assembly: every documented opcode incl. CB/ED/DD/FD/DDCB/FDCB (DDCB/FDCB through the CB table with a precomputed (IX+d) operand pointer), R register, HALT, IN/OUT as no-ops (IN answers 0), RST 08h/10h handoff at BIOSpage/BDOSpage, sliced execution with all state in package statics; register plan AL/AH=A/F, CX=BC, DX=DE, DI=HL, SI=PC, BP=Z80 SP (ds:), BX=dispatch scratch
- CP/M 2.2 memory image exactly as _PatchCPM builds it (60K layout: BDOS 0xEC00, BIOS 0xFE00, CCP at 0xE400 - globals.h's CCP_DR block, TPASIZE*1024-1024), warm boot, DSKByte, IOByte
- BIOS: BOOT (-> STATUS_EXIT), WBOOT, CONST, CONIN, CONOUT, LIST and AUXOUT/PUNCH as no-ops (as cpm.h), READER (0x1A), HOME/SELDSK/SETTRK/SETSEC/SETDMA/READ/WRITE/LISTST/SECTRAN as in cpm.h
- BDOS 0-40 (console 1,2,6,7,8,9,10,11,12; 4/5 capture to A\0\PUN.TXT / LST.TXT; disk 13-40, F_ATTRIB answers 0 as upstream), plus RunCPM-private 230 (8-bit mask), 231 (F_BDOSCALL -> 0), 248 (uptime ms from ticks x55), 249 (MakeDisk = mkdir), 250 (HostOS = 0x08), 251/252/253, 254 (accepted, no-op)
- BDOS 10 line editor with RunCPM's full ^A..^X/^? key map and help text, as a state machine driven by os88_onkey; ^W recall buffer 256 bytes static
- Drives = folders A\0 .. P\F below the launch folder; user areas; SELDSK of an absent DRIVE folder -> 'Bdos Err on X: Select' (the letter alone decides, as _sys_select does; an absent user folder is empty, never Select); USER n creates the folder at once (_SetUser -> _MakeUserDir), in wave 4; FORMAT.COM/BDOS 249 creates a drive folder; the (drive,user) switch is one quiet goto that also moves the instance (api_gaps)
- FCB file model over whole files: 8-entry open-file table in heap claims, sequential and random read/write, extents/rc/s2 dirty bit, F_SFIRST/F_SNEXT from a 128-entry directory cache with '?' matching and the multi-entry extent chain for large files, ERA/REN/MAKE, $$$.SUB (SUBMIT.COM works), files up to 65,535 bytes
- Console: VT100/ANSI subset (CR LF BS BEL TAB FF; ESC[ H f A B C D J K m(0/7) L M s u ?25h/l) over an 80x25 model (RC_ROWS = 25; rows shown = what the window holds: 25 VGA/Hercules framed, 17 CGA framed, 25 CGA fullscreen), reverse-video attribute, cursor as an inverse cell, keys incl. arrows/Home/End/PgUp/PgDn/Del as VT sequences, ^C, 7-bit mask
- Damage-only glass: shadow, per-row diff, gfx_scroll per scrolled line plus one white fill of the vacated rows, one composed band + gfx_blit1 per changed row at every width (measured cheaper than one font_run even for one cell, PERFORMANCE.md Set 65; font_run is only the blit1-refused fallback), a cursor moved 8+ cells along an unchanged row as two 1-cell bands, full repaint only on expose and then as one fill of the content plus each row to its last non-blank cell; os88_onwake holds the gfx lock only around the flush
- DRI CCP (CCP-DR.60K) as the command processor, loaded once at launch (before any folder move) into a 2KB claim and copied to 0xE400 on every warm boot; CCPHEAD printed before each; AUTOEXEC.TXT read from the launch folder once at launch and poked at CCPaddr+7/+8 on every warm boot (the same behaviour as upstream's read-per-boot: CP/M cannot reach the launch folder)
- EXIT.COM / BIOS BOOT: prints the RunCPM exit text, then a 10-line self-close worker (cword's File > Close idiom) closes the window; HALT toasts 'RunCPM: CPU halted' and does the same
- Boot banner with a measured 'Estimated Z80 clock speed: N.NN MHz' (16-bit tick arithmetic over a fixed burst), 'CPU is 8086 native', 'Built <pinned commit date>'
- About box (product, 6.9, MIT attribution to Mockba the Borg / Marcelo Dantas, what this port is, OK) <= 12 rows; kernel bar name 'RunCPM' via an empty menu set
- Fullscreen latch (Decision 2): a chord the Z80 side cannot want (Alt+F, scan code with ascii 0) enters/leaves WF_FULL - 80x25 on every adapter, stated in SPEC as the exception to 11.2.1 with its reason
- Disks: build/runcpm.img/720/360 (wave 2) carrying RUNCPM.O88 (+ .OVL if split) + CCP-DR.60K in the root beside A\0\; A\0 = the master-disk executables (.COM/.SUB/.TXT/.ME on 360KB; sources on 720KB/1.44MB as far as they fit, manifest listed; no file above 65,535 bytes on any geometry, the readme names the three left off); RUNCPM\ folder on apps-all.img; three geometries, each --verify'd
- vm/386-runcpm 86Box machine (wave 2); XT numbers measured on vm/xt640 (640KB) with fdd_02_fn hand-pointed for the session; xt-runcpm only if the measured speed justifies it
- Launch requirement defined by claims, not KB: os88_mem_claim(64) for Z80 RAM + the CCP claim + one file claim must succeed; the refusal sentence quotes what was asked and os88_mem_largest_kb()

### Present and greyed (SPEC.md §47 — the fact that greys it)

- **CPU throttling / BDOS 254 (F_CPUSPEED) and cpuDelayInstructions** — the fact is structural, not a guess about speed: nothing in this design can sleep inside a slice (the emulator runs on the UI task and never blocks), so there is no delay for 254 to act on - accepted, ignored; the banner prints the speed the machine delivers, whatever it is (a 486/Pentium 86Box machine may well exceed a 4 MHz Z80)
- **files larger than 65,535 bytes (open, make, random write past 64KB)** — the os8088 file API a package reaches reads and writes whole files with a 16-bit count and no seek; BDOS answers 0xFF/error 6 and a toast names the file and '64KB'; DIR still lists such a file with its full extent chain; the three such files on the master disk are not shipped and the disk readme says so
- **more than 8 files open at once** — each open file is a heap claim; the 9th open flushes/evicts a clean entry or errors 'Bdos Err on X: CP/M ERR' with a toast 'RunCPM: 8 files open' (24 characters is the toast's cap, SPEC.md 74.3)
- **terminal geometry in a FRAMED window: 79 of 80 columns on VGA and CGA (measured, §74.2: the 640-wide window is flush with the screen, no left border, and 639 px hold 79 cells; Hercules' 720 px shows all 80), 17 of 25 rows on CGA (200 - bar - dock - title = 138 px)** — these are facts of the window frame, not of the display: the fullscreen latch (Decision 2) shows 80x25 on every adapter; if it is not built the greying reads 'fullscreen was not built because <the user's reason>'
- **ANSI bold/underline/colour (ESC[1m, 4m, 3x/4xm)** — one 8x8 face, no true bold, and 1bpp adapters round every colour to black/white/dither - only reverse video (7m) is rendered; others are parsed and ignored
- **host escape sequences not in the subset (VT52, DEC private modes other than ?25, scroll regions)** — RunCPM contains no terminal emulator; this port carries the subset TE/WordStar/CLS use; unknown sequences are swallowed
- **CP/M 3 (CPM3), banked memory (BANKS>1), F_MULTISEC, date stamps, DATE command** — off by default upstream and needing long/time(); this build is CP/M 2.2 (BDOS 12 answers 0x0022 as upstream does)
- **XMODEM.COM / serial transfer** — no serial path reaches a package in this OS; files are put on the floppy from a host that mounts FAT12 or copied in the Disk window
- **Z80estimateClock's uint64 method** — no 32/64-bit type; replaced by a 16-bit tick count over a fixed instruction burst - same two output lines (with two decimals and smaller T-state magnitudes, both stated), measured on this machine
- **INFO.COM's host name** — the shipped INFO.COM's table (INFO.Z80 41-95) predates HostOS 0x08 and prints 'running on Unknown' here; the binary is third-party and is not edited
- **HALT message** — '::CPU HALTED::' is a DEBUG-only string upstream; the shipping build is silent, and this port toasts the fact through the OS before closing

### Absent

- Internal C CCP (ccp.h) - host-blocking C; the DRI binary CCP is the command processor. Its extra commands (LDIR/COPY/DEL/ECHO/PAGE/POKE/VER/DUMP/VOL/CLS/?) do not exist at the prompt; PIP/DUMP/EXIT are .COM files on the master disk
- Debugger/disassembler/trace/logs (debug.h, DEBUG/iDEBUG/DEBUGLOG) - not in a shipping upstream build; ~10KB of strings
- Arduino GPIO BDOS 220-224/232, hardware IN/OUT, LED codes, SdFat, Serial console
- STREAMIO console redirection, RunVT embedding, getopt options
- ABDOS alternate BDOS
- cpu2/3/4 alternate cores - one 8086 core
- AUTOEXEC BOOTONLY variants beyond upstream's default (every warm boot); PROFILE builds
- Z80ASM.PDF, BDOS.ASM, ZCPR3.ASM (the three master-disk files above 65,535 bytes) - not shipped on any geometry; BDOS.SUB and ZCPR3.SUB therefore cannot be run, and the disk's LEFT-OFF.TXT says so (derived from the .SUB files at build time)

## Files

| file | holds | resident |
|---|---|---|
| `apps/runcpm/runcpm.c` | the one translation unit: MIT header, prototypes at top, #includes of the parts below (ALL created as stubs in wave 1 so the Makefile prerequisites are written once), os88_main (64KB Z80 claim + CCP claim loaded BEFORE any folder move, one ovl_* touched here if an overlay exists, window 640x(18+200) authored for 640x480 and clamped, wm_snap, empty menu set 'RunCPM', about_set, onwake install, launch place recorded), os88_paint (full redraw from the model on expose), os88_onkey (key ring push + line-editor advance + kick; Alt+F fullscreen chord), os88_onwake (the slice driver: run N instructions by CPU class, service the trap, drain the console, lock/flush/unlock, re-post), os88_oncmd stub, os88_about, os88_worker (self-close only), the boot state machine STATUS_RUNNING/EXIT/RESTART, banner and clock estimate, the always-compiled debug key that loads a named .COM from the launch folder to 0x100 (wave 2's loader) | yes |
| `apps/runcpm/rcterm.c` | terminal model (chars stride 80 via row<<6 + row<<4, attrs as a reverse-video BITMAP, 25 rows x 80 cols), VT100/ANSI parser, cursor, scroll count, glass shadow and the damage flush (rc_rowdiff -> rc_band + gfx_blit1 at every width, font_run only as the blit1-refused fallback; gfx_scroll + one fill of the vacated rows, the shadow kept true on a refusal), key-to-sequence mapping table | yes |
| `apps/runcpm/rccpm.c` | _PatchCPM (ovl_ candidate), _Bios and _Bdos dispatchers minus CPM3/Arduino/DEBUG, C_READSTR as a state machine with the same key table, console helpers (mask8bit, puthex8/16, putdec), private calls 230/231/248-254; the keystroke-path functions (1,2,6,9,10,11, F_READ/F_WRITE) resident | yes |
| `apps/runcpm/rcfs.c` | REWRITTEN disk layer: place table (drive,user)->folder cluster resolved lazily by os88_file_find + the instance-marking quiet goto (fallback os88_file_goto); 128-entry directory cache per current (drive,user) with in-place updates on our own create/delete/rename; 8-entry open-file table over heap claims (open loads whole file with os88_file_read_seg, close writes back with os88_file_write_seg when dirty, regrow on growth); FCB<->name, match(), _mockupDirEntry with the multi-extent chain (two-word size math), seq/random record math in 16 bits, $$$.SUB, PUN/LST as ordinary OFT entries flushed at warm boot, _error path; F_MAKE/ERA/REN/MakeDisk/_error text are ovl_ candidates | yes |
| `apps/runcpm/rcabout.c` | the About panel (<= 12 rows, control y = 6+row*10) and its OK/Esc dismissal, shadow invalidation after it comes down; the first ovl_ candidate | overlay candidate |
| `apps/runcpm/rcz80.inc` | HAND-WRITTEN Z80 core: _rc_run(n) cdecl entry that saves DS/ES/BP, loads DS = Z80 claim, unpacks registers from cs: statics (AF->AX, BC->CX, DE->DX, HL->DI, PC->SI, SP->BP), fetch `xor bh,bh / mov bl,[si] / inc si / shl bx,1 / jmp [cs:bx+tab]`, lahf-layout F with OF->P/V and N fixups, per-prefix tables (base, CB, ED, DD, FD; DDCB/FDCB via the CB table with an operand pointer), RST 08/10 -> return with reason, HALT, slice count; ~2,500-3,500 lines | yes |
| `apps/runcpm/rcmem.inc` | the ES-loading movers, marked cc8086:allow: rc_rd(a) rc_rd16(a) rc_wr(a,v) (near, ~5us), rc_zcopy_in(zaddr, src, n), rc_zcopy_out(dst, zaddr, n), rc_zzcopy(seg,off -> zaddr / zaddr -> seg,off, n) for the 128-byte DMA and 36-byte FCB and CCP images, rc_zfill; tested by hosttest/rcmemtest (boot-sector harness, cwmovetest precedent) | yes |
| `apps/runcpm/rcband.inc` | rc_band(chars, attrbits, ncells) composes one 8-row 1bpp band from OSAPI_FONT_GLYPHS (8-aligned cells: byte copies, XOR 0xFF for reverse) into a static 80x8 band; rc_rowdiff(a, b, n) -> first/last differing cell; both called once per row | yes |
| `apps/runcpm/runcpm.asm` | the shim: CC_PKG_NAME 'RUNCPM', CC_HAS_ONKEY, CC_HAS_MENUS, CC_HAS_ABOUT, CC_HAS_ONWAKE (new), CC_HAS_WORKER, CC_HAS_OVL if split, CC_ICON, %include cc/crt0.asm, runcpm.gen.asm, runcpm/rcz80.inc, runcpm/rcmem.inc, runcpm/rcband.inc, CC_IMAGE_END | yes |
| `apps/runcpm/build.sh` | runs the host checks (rcuitest, rcfstest) before the 8086 build, cword's build.sh precedent; invoked by the make rule | yes |
| `apps/runcpm/hosttest/os88.h` | the stub header (second copy of apps/cc/os88.h's shapes without the poison) + prototypes/stubs of rc_* asm shims, os88_wm_wake/onwake, os88_file_goto_q_mark, os88_snd_tone, fullscreen; grows in every wave that adds an API | yes |
| `apps/runcpm/hosttest/rcuitest.c` | the glass model harness: cellch/pixline arrays, font_run/blit1/scroll modelled (not refused), stubs for the fs and the wake, feeds byte streams (banner, DIR listing, ESC sequences, a TE-style 25-row full-screen redraw) to the terminal, verifies model == glass, audits the shadow, prints the cost table (echo one char, one DIR row, one scrolled line, ESC[2J, full 25-row repaint, cursor-only move) | yes |
| `apps/runcpm/hosttest/rcfstest.c` | rcfs.c against a fake folder tree: FCB<->name, wildcard match, dirent synthesis for 0/127/128/16384/16385/65535/67301/134899-byte files (the last two: multi-entry chain listed, open refused), seq/random record math, OFT eviction, $$$.SUB order | yes |
| `apps/runcpm/hosttest/rcmemtest.asm + rcmemtest.sh` | boot-sector QEMU harness running rcmem.inc with SS != DS and negative controls (ES not restored) | yes |
| `tests/rczex.py + tests/rczex_ocr.py` | the Z80 core gate as top-level test drivers (the tests/*.py convention): boots build/runcpm.img in QEMU, launches RUNCPM by the two-press double-click, types ZEXDOC, reads the terminal rows off screendumps through an 8x8-glyph OCR harvested once from a known string, until 'Tests complete'; ZEXALL on demand; the measured ZEXDOC runtime written into the recipe | yes |
| `tools/getruncpm.py` | fetches MockbaTheBorg/RunCPM at a pinned commit into build/runcpm-src, unpacks DISK/A0.zip into build/runcpm-disk/A/0 (dropping the three files above 65,535 bytes, listing them), copies CCP/CCP-DR.60K unless it is committed (Decision 3); nothing third-party committed beyond that (tools/getstories.py precedent); a stamp file the disk rules and allapps depend on | yes |
| `kernel: events.inc (EVT_WAKE), ui.inc dispatch, wm.inc (W_ONWAKE word or install slot), kernel.asm API table (OSAPI_WM_WAKE, OSAPI_WM_ONWAKE, and the instance-marking variant of osapi_file_goto_q); apps/os88api.inc; apps/cc/crt0.asm (CC_HAS_ONWAKE trampoline); apps/cc/os88thunk.asm + os88.h (os88_wm_wake, os88_wm_onwake, os88_file_goto_q_mark, os88_fullscreen)` | the wake mechanism and the quiet-goto fix (Decision 1), the fullscreen thunk (Decision 2) and the dozen-line thunks; kernel headroom today: image rung 429 bytes left, footprint 1,024 spare (kernsize[big]) | yes |
| `vm/386-runcpm/86box.cfg; Makefile (CC_PACKAGE runcpm[,RUNCPM.OVL], RUNCPMSRC prereqs for every .c and .inc written once in wave 1, three disk rules + runcpm-src stamp, allapps lines, rczex, 386-runcpm target); SPEC.md new top-level section for RunCPM (cited from 73.12's neighbours); README` | machine, build, docs | yes |

## Budget

- image estimate: 42000
- bss estimate: 11500
- overlay estimate: 6000

cword all-in ratio: build/cword.bin 54,450 (35,886 resident + 18,564 .ovl) for ~8,046 compiled lines = ~5.9 bytes/line (the draft's 3.7 dropped the overlay). C: runcpm.c ~600 + rcterm.c ~600 + rccpm.c ~1,100 + rcfs.c ~1,100 + rcabout.c ~200 = ~3,600 lines = ~19-24KB. Assembly: rcz80.inc ~3,000 lines = ~7-10KB handlers (ALU handlers ~12 bytes each with the OF->P/V and N fixups) + 5 x 512-byte dispatch tables (base, CB, ED, DD, FD; DDCB/FDCB share CB) = 2.5KB -> 9.5-13KB; rcmem/rcband ~400 lines = ~1KB. crt0 runtime + thunks 5-6KB (ccsmoke alone is 3,406); string literals ~1.5KB (banner, CCPHEAD, C_READSTR help, error texts); icon 128 B. Total 36-47KB image; ~42KB is the planning figure. bss: terminal model chars 25x80 = 2,000 + attr bitmap 250; glass shadow 2,000 + 250; band 640; key ring 64 + line editor 128 + 256 recall; OFT 8 x 24 = 192; directory cache 128 x 16 = 2,048; place table 16x16 words = 512; scratch names/paths 128; About/misc ~500; DDCB operand pointer and core statics ~64 = ~11.5KB. Sum ~53.5KB of 61,440 - AT the 55,000 trigger, so LESSONS 2 applies: before wave 2, assemble the dispatch tables + stub handlers and compile stubbed rccpm/rcfs/rcterm to get os88pkg's size line and plan against it. ovl_* candidates named now (rcabout.c, banner + clock estimate, _PatchCPM, F_MAKE/ERA/REN/MakeDisk/_error text, name<->FCB conversion for search) ~6KB; the slice loop, terminal flush, BDOS 1/2/6/9/10/11 and F_READ/F_WRITE stay resident. If an overlay exists: CC_PACKAGE third argument, one ovl_ touched in os88_main before the first folder move (the .OVL resolves in the instance's folder, crt0.asm 745-747), and RUNCPM.OVL beside RUNCPM.O88 on every disk. Heap: 64KB Z80 RAM + 2KB CCP + overlay claim + up to 8 x <=64KB open files; launch = the three claims succeeding, refusal quotes os88_mem_largest_kb().

## API gaps

- **need:** a way for a package to be called back on the UI task without a user event - the emulator's slice loop, console flush and every disk-touching BDOS call run there (file slots are UI-task only; a worker's stack is 256 bytes)
  - **slot:** none exists (evq has only mouse events; no timer/tick callback; W_PAINT/W_ONKEY only on user action - and W_ONKEY is dispatched UNDER the gfx lock at kernel/ui.inc:107, so it cannot carry the wake)
  - **action:** add to the kernel: EVT_WAKE record {type, a = win ptr}; OSAPI_WM_WAKE (BX = win; evq_push, ISR/worker-safe, CF=1 if the queue was full) and OSAPI_WM_ONWAKE (install a near proc, like WM_ONMOUSEUP; one word in the window record or the instance record); the UI task's event loop dispatches it through the package dispatcher WITHOUT the gfx lock. crt0.asm CC_HAS_ONWAKE trampoline; os88.h/os88thunk.asm: void os88_wm_onwake(void *win); int os88_wm_wake(void *win). ~100-150 kernel bytes against a measured 429 left in the image rung and 1,024 footprint spare - fits without a rung crossing if it lands in .text. This is Decision 1
- **need:** switch between drive/user folders on every BDOS call without a remount, listing, sort or icon harvest - AND have the next file call resolve there
  - **slot:** OSAPI_FILE_GOTO_Q (os88api.inc:2227) moves the machine but deliberately not the instance (kernel.asm ~2977, SPEC 19.2.2), and every package file cell runs inst_vol_enter first (kernel.asm 2243/2328/2446), which re-stands the machine in the instance's folder - so goto_q alone is undone by the next read
  - **action:** fold into the wave-1 kernel work: a variant (a flag bit in BH, or a second slot) that does the quiet stand and then inst_vol_mark, thunked as os88_file_goto_q_mark(unsigned clus, int vol); FILE_FIND then fills the directory cache in the same folder. Fallback if the kernel change is refused: os88_file_goto (already thunked) once per (drive,user) change - a full dsk_chdir per switch, restated in the risks. Either way the CCP load and any overlay load happen before the first move, and the launch place is recorded
- **need:** the kernel 8x8 glyph table for composing a row band
  - **slot:** OSAPI_FONT_GLYPHS (os88api.inc:1222) - not thunked
  - **action:** used directly from rcband.inc in the shim's assembly (DX:SI); no os88.h change
- **need:** byte and block access to the 64KB Z80 RAM claim from C
  - **slot:** os88_peek/os88_poke exist but are far calls (~47us) - a program, not a peek
  - **action:** write it in the package: rcmem.inc near cdecl accessors and ES-loading movers marked cc8086:allow, tested by a boot-sector harness (cwmove precedent)
- **need:** the whole 80x25 on every adapter (CGA framed shows 17 rows, 640-px framed shows 79 columns - measured, §74.2)
  - **slot:** OSAPI_FULLSCREEN (os88api.inc:315, SPEC 11.2 - the WF_FULL window latch, not the 53 bracket) - not thunked
  - **action:** Decision 2: thunk int os88_fullscreen(void *win, int enter) (a dozen lines; caller holds the gfx lock, which a callback does) and bind a chord the Z80 side cannot want (Alt+F) in both directions, stated in SPEC as the exception to 11.2.1; or grey with the framed-window fact and the user's reason
- **need:** files larger than 65,535 bytes
  - **slot:** OSAPI_FILE_READ_AT (os88api.inc:2208) exists, not thunked; writes have no counterpart
  - **action:** decided: grey the feature (BDOS error + toast fact), ship no such file, list the extent chain in DIR; the read-side thunk is not added
- **need:** self-close on EXIT / BIOS BOOT / HALT
  - **slot:** none (no self-close slot)
  - **action:** write it in the package: the cword idiom - a worker that sleeps 4 ticks, os88_gfx_lock, os88_wm_destroy, unlock, os88_task_alive
- **need:** terminal bell (BEL)
  - **slot:** OSAPI_SND_TONE (wrapped)
  - **action:** os88_snd_tone(880, 2, 0x40)

## Waves

### Wave 1 — Window, terminal model, glass shadow, banner, harness, the wake mechanism and the quiet-goto fix

- SPEC section written first (its own top-level section, cited from 73.12's neighbours; every number 'measured: pending', no dangling references so checkdocs passes from the first commit): the RunCPM package, the wake event, the goto_q_mark variant, the terminal geometry (80x25 model; 79/79/80 columns framed on VGA/CGA/Hercules - measured, §74.2, 17 rows CGA framed), the file model, the three banner deviations, the keyboard note (^? = Ctrl+-, DEL = Ctrl+Backspace)
- kernel: EVT_WAKE + OSAPI_WM_WAKE + OSAPI_WM_ONWAKE + UI dispatch without the lock; the instance-marking goto_q variant; crt0 CC_HAS_ONWAKE; thunks os88_wm_wake/os88_wm_onwake/os88_file_goto_q_mark (+ os88_fullscreen if Decision 2 says yes); kernsize before/after quoted against the 429/1,024 headroom; a counter in the RunCPM window proves the round trip (a wake handler that re-posts N times and shows N)
- all five .c files created (stubs where empty) with MIT + Mockba the Borg / Marcelo Dantas / v6.9 headers, and the Makefile's RUNCPMSRC and .bin %include prerequisite lines written once for every .c and .inc; apps/runcpm/build.sh; a bare build/runcpm.img rule (RUNCPM.O88 only, --verify'd) for make test TESTAPPS=
- os88_main: claims (refusal sentence quoting largest_kb), window 640x(18+200) at (7,20), wm_snap, empty menu set 'RunCPM', about_set, onwake install; os88_paint = full redraw from the model with the shadow invalidated; os88_onwake locks only around rc_flush
- rcterm.c: 25-row model (stride 80, attr bitmap), VT100 subset parser, cursor, scroll counter, shadow, damage flush (bands; gfx_scroll + one fill of the vacated rows); rcband.inc + blit1 path with font_run fallback when blit1 answers -1
- banner text from main.c pushed through the terminal at first paint
- hosttest/os88.h + rcuitest.c: glass model, byte-stream driver, model==glass verify, cost table skeleton, a TE-style 25-row redraw script

**Files:** `apps/runcpm/runcpm.c`, `apps/runcpm/rcterm.c`, `apps/runcpm/rccpm.c (stub)`, `apps/runcpm/rcfs.c (stub)`, `apps/runcpm/rcabout.c (stub)`, `apps/runcpm/rcband.inc`, `apps/runcpm/runcpm.asm`, `apps/runcpm/build.sh`, `apps/runcpm/hosttest/os88.h`, `apps/runcpm/hosttest/rcuitest.c`, `kernel/events.inc`, `kernel/ui.inc`, `kernel/wm.inc`, `kernel/kernel.asm`, `apps/os88api.inc`, `apps/cc/crt0.asm`, `apps/cc/os88thunk.asm`, `apps/cc/os88.h`, `Makefile`, `SPEC.md`

**Done when:** make runcpm builds and os88pkg prints the size line; kernsize shows the wake + goto_q_mark bytes inside the 429/1,024 headroom (or names the crossing for Decision 1); make test TESTAPPS=build/runcpm.img shows the RunCPM window with the banner lines in the 8x8 face; a screendump cropped at zoom 4 shows the wake counter climbing without any key; a goto_q_mark to a subfolder followed by os88_file_read of a file that exists only there succeeds; the harness prints echo-one-char = 2 calls/3 cells, one scrolled DIR row = 2 calls (scroll + blit1) or 2 calls/78 cells on the fallback, full repaint = 25 calls; the VGA screendump shows 79 columns (measured, §74.2) and 25 rows, VIDEO=cga shows 17 rows and 79 columns with nothing off the content box

**Wave 1 — measured (2026-08-17, after two review passes):** `os88pkg: 'RUNCPM' image=12590 bss=6473`, 42 functions, largest frame 30. Kernel: +181 `.text` / +44 `.bss`, image rung 429 → 204 left (kern_small 255 → 30), no budget moved; slots 0x0428 `OSAPI_WM_WAKE`, 0x0430 `OSAPI_WM_ONWAKE`, 0x0438 `OSAPI_FILE_GOTO_QM` (SPEC.md §74.1 has the table). Where the glass disagreed with the plan's estimates, the glass wins and §74.2 carries the numbers: a 640-px framed window shows **79** columns, not 78 (it is flush with the screen and has no left border, §11.95.2); echo one character is **1 call / 2 cells** (the character and the moved cursor are one band — the cursor is folded into the row, never a call of its own; the attribute compare is bit-exact, so a moved cursor is the two cells and never their 8-cell groups); a cursor move to another row 2 calls / 2 cells (a 1-cell band each - the band is the first choice at every width, font_run only the blit1-refused fallback), along an unchanged row two 1-cell bands when the span would be 8+ cells and one span when nearer (the measured crossover); a scrolled DIR row is **4** calls / 61 cells (scroll + ONE fill of the vacated 8-px strip + the text row + the cursor's 1-cell band on the vacated row), a band per row from the top on the scroll-refused fallback (the shadow stays true - nothing moved); ESC[2J is 2 (one fill + the cursor) and a cursor move on the still-blank screen after it is 2 calls and NO refill (the flush consumes the all-blank fact); a TE-style 25-row redraw is 25; a full expose is ONE fill of the content plus each row to its last non-blank cell (the banner screen 9 calls / 218 cells, a full TE screen 25); Alt+F in either direction is the latch's own synchronous paint and then 0 (the harness stub paints as wm_fullscreen does and asserts it). The band was MEASURED after review (tests/rcband, `make rcbandbench`, PERFORMANCE.md Set 65): 860 us a call + 173 us a cell on the icount harness, the first loop 306 us a cell (7x the model's ~40 guess) before the rewrite; on that model a scrolled line is ~47 ms of which ~31 ms is the 200-row gfx_scroll, so wave 2's slice pacing (lines per flush) is a performance decision - see wave 2. The framed VGA window is TITLE_H + 200 + 1 tall: at +200 it showed 24 rows and a sliver, and the model's row 24 - a scrolled cursor's row - was never on the glass (found on the glass after review, not by the harness). The banner's memory layout was wrong in the first build and every document (BDOS 0xE400 / CCP 0xDC00): globals.h's CCP_DR block gives BDOSjmppage = 60×1024−1024 = **0xEC00**, BDOSpage 0xED00, CCPaddr = BDOSjmppage−0x800 = **0xE400**, and CCP-DR.60K's own JP/CALL targets (0xE4xx–0xEBxx) confirm it - corrected everywhere, derived from globals.h. The 'Built Jul 21 2026 - 20:43:19' line is the pinned upstream commit e698e8ab59c2de915b23be7f5b146a5c621f5c76's date; tools/getruncpm.py (wave 2) pins the same hash. Alt+F arrives as ascii 0 / scan 0x21, is not consumed by the kernel, and latches both ways on VGA and CGA (CGA fullscreen = 80×25 exactly). `wm_wake` coalesces (one queued wake per window), which the plan did not ask for and which makes 'kick from every callback' safe against the 16-record ring. The wave-1 disk is `build/runcpm.img` only (RUNCPM.O88 + `A\0\RCPROBE.TXT`, the goto_q_mark probe's file); the 720/360 geometries come with the master disk in wave 2. Two pieces of scaffolding are in `runcpm.c`, marked RC_W1, for wave 2 (the wake counter row and the local echo — `rc_slice()`) and wave 4 (the probe in `os88_onwake`) to remove.

### Wave 2 — The Z80 core, the slice loop, the disks, the machine and the core gate

- LESSONS 2 first: assemble the dispatch tables + stub handlers with the stubbed C and read os88pkg's size line; if it projects past 55,000 resident, the ovl_* names are applied now and CC_PACKAGE gets its third argument
- rcz80.inc: full documented Z80 instruction set on the corrected register plan (AL/AH, CX, DX, DI=HL, SI=PC, BP=SP with ds:, BX dispatch), lahf-layout flags with fixups, prefixes incl. DDCB/FDCB via the CB table, R, HALT, RST 08/10 handoff returning a reason code, slice count; registers unpacked/packed in cs: statics; DS = Z80 claim inside, restored on exit
- rcmem.inc movers + rcmemtest boot-sector harness with negative controls
- os88_onwake slice driver: N instructions by os88_cpu() class (~50 ms on the target), status machine, kick from paint/key/click so a dropped wake cannot stall the machine, re-post ONLY while the machine has work (the slice ran out with the Z80 running, or output is pending) and never while blocked in CONIN on an empty key ring - os88_onkey kicks then (§74.1's handler contract; wave 1's unconditional re-post is RC_W1 scaffolding to remove); and the flush pacing is a PERFORMANCE decision: on the harness model a scrolled line is ~40 ms of which ~30 is the 200-row gfx_scroll (§74.2), so flush every N lines or when the key ring is drained, with N sized so a screenful of TYPE is a handful of scrolls, and measure it
- a minimal BDOS 2/9 and CONIN-retry path plus the always-compiled debug key that os88_file_read_seg's a named .COM from the launch folder into a scratch claim, rc_zzcopy's it to 0x100 and runs it (the loader wave 3/4 do not yet provide)
- tools/getruncpm.py + build/runcpm-src stamp; disk rules build/runcpm.img/720/360 carrying RUNCPM.O88 (+ .OVL) + CCP-DR.60K + build/runcpm-disk/A/0 (no file > 65,535 bytes), each --verify'd; vm/386-runcpm (copy of vm/386-c-word with B: image and uuid changed) + Makefile target
- tests/rczex.py + rczex_ocr.py: glyph OCR harvested from a known screendump string; ZEXDOC timed once on the first working core in QEMU and the figure written into the recipe; the banner's measured clock estimate

**Files:** `apps/runcpm/rcz80.inc`, `apps/runcpm/rcmem.inc`, `apps/runcpm/runcpm.c`, `apps/runcpm/hosttest/rcmemtest.asm`, `apps/runcpm/hosttest/rcmemtest.sh`, `tests/rczex.py`, `tests/rczex_ocr.py`, `tools/getruncpm.py`, `vm/386-runcpm/86box.cfg`, `Makefile`, `SPEC.md`

**Done when:** a hand-assembled Z80 hello (a .COM on build/runcpm.img loaded by the debug key) prints through BDOS 9; ZEXDOC loaded the same way reports every group OK in QEMU (make rczex; if the measured run is over ~15 minutes the wave gate is the hello plus a per-opcode-group host check and the full ZEXDOC becomes wave 4's gate); the banner prints an 'Estimated Z80 clock speed' line, and the number read on vm/386-runcpm and on vm/xt640 (fdd_02_fn hand-pointed at build/runcpm360.img for the session, cfg git-checkout'ed after) is recorded in SPEC; the size line after the stub build is recorded and the resident/overlay decision written down

**Wave 2 — measured (2026-08-17, re-measured after two reviews):** `os88pkg: 'RUNCPM' image=24848 bss=6465`, 58 functions, largest frame 30 — the core, its tables and the movers cost ~9.6KB of image and ~50 bytes of bss (the LESSONS 2 stub build was skipped in favour of the real one: with the whole core in, the line is 24.5K + 6.5K = 31K against the 55,000 trigger, so no `RUNCPM.OVL` yet and the ovl_* candidates stand as named for the wave whose size line asks). The core (`rcz80.inc`, ~1,900 lines) is on the corrected register plan; the slice budget counts CONTROL TRANSFERS, not instructions (the decrement lives only in JP/JR/DJNZ/CALL/RET/RST and the block-repeat step, so the straight-line path never touches memory for it); the CB set is a compact getter/operator/putter (three indirections, ~600 bytes) because 8080-era software never issues one; the undocumented IXH/IXL/IYH/IYL forms and SLL ARE implemented because ZEXDOC exercises them (the first run's eight failing groups were exactly those plus DAA - `test` after `sahf` had cleared the carry DAA reads, and DAS sets C on the low nibble's borrow where the Z80 does not; DAA/CPL/SCF/CCF were then probed over every A x H/N/C against cpu1.h's rule, 4,096 cases each, 0 mismatches). **ZEXDOC: 67 of 67 groups OK** in the raw-QEMU boot-sector harness (`hosttest/rcz80test.sh`, the same shipping core, SS != DS, 144 s) - the raw harness was written FIRST as the development loop and is `make rcz80test`; the in-OS gate `tests/rczex.py` (`make rczex`) launches by the two-press double-click, loads through Alt+L and reads the terminal off screendumps with `tests/rczex_ocr.py` (glyphs harvested from the banner and the group names, origin found by consistency: (0,38) on VGA); **179 s from Alt+L to 'Tests complete', 67 of 67 OK; 146 s after the review's slice fixes** (175-211 s over three runs after the second review, on a host at load ~2.3 where the raw harness took 155 s and a control build with the previous adaptation 178 s: the host's spread) (the first, fixed-slice run was ~5x the raw figure - the wake round trip and the per-slice flush - which is why the slice length ADAPTS: os88_cpu() sets 512/1024/2048, four slices inside one tick double it to a 16,384 cap, a slice across two tick boundaries halves it to a 256 floor; ~14-55 ms a slice on every machine - and ONLY an EXHAUSTED slice is timed: the second review found that a slice ending early in a blocked C_READ counted as fast, so ~30 keystrokes of ordinary typing into TE walked the budget to the cap and the next busy slice was ~1.6 s of UI task; rc_slice/rc_clock_step now answer whether the budget was spent and an early end leaves the estimate alone, which the harness asserts both ways). The clock estimate is cpu_mhz.h's loop with BC=1000/DE=10 (260,319 T-states a burst by its own table) run THROUGH THE ORDINARY SLICES - a slice a wake, a burst resumed across wakes, PC back to 0000 only after a HALT, only completed bursts counted, t0 at the first slice - until 4 ticks pass and a burst has completed (the first build ran a whole burst per wake with rc_run(0x7FFF): ~11,000 control transfers, ~1 s of UI task with nothing dispatched on the target - the review caught it, and the harness now models a burst that spans slices and fails a restart): two words for the T-states, ticks x 55 for the ms, two decimals - QEMU on this host says ~157 MHz-equivalent (the first build's 30-32 was t0 taken in os88_main before the window's first paint; a fact about the host either way). THE 386 AND XT FIGURES ARE PENDING and the 8086 slice length is therefore UNMEASURED (SPEC.md 74 says so): 86Box is a GUI application the sessions could not drive (no assistive access for input), and MartyPC - docs/TESTING.md's instrument for the 8088 - needs a Rust toolchain and an IBM ROM this host does not have; the two readings are the user's to take (`make 386-runcpm`; vm/xt640 with fdd_02_fn hand-pointed at build/runcpm360.img, cfg git-checkout'ed after) and go into SPEC.md 74 with the slice constants they justify. The movers (`rcmem.inc`: rd/rd16/wr/wr16, zcopy in/out, zzcopy in/out between claims, zfill; ES restored, DF clear, `cc8086:allow` on every rep) pass `hosttest/rcmemtest.sh` (boot sector, SS != DS, the 64KB wrap, guards, two negative controls caught) which `build.sh` runs before every build. `rccpm.c` carries `_PatchCPM` whole (DPB/DPH byte for byte), the console side of `_Bdos` (0-2, 6-9, 11-12; disk functions answer 0xFF until wave 4) and `_Bios` (BOOT/WBOOT/CONST/CONIN/CONOUT and every register-only entry incl. MOVE); a console read on an empty ring puts PC back on the RST and blocks the machine (`RC_M_BLOCKED`, no re-post) until os88_onkey kicks. The debug loader (Alt+L, scan 0x26) reads NAME.COM from A\0 through `rc_fs_cd(0,0)` - the wave-1 probe and RCPROBE.TXT are gone, this is the living proof - or from the launch folder (not when A\0's copy was merely too big: the fallback's NOENT would hide the fact), into the claim at the first 512-ALIGNED offset at or above 0200h and then rc_zzcopy_in down to 0100h - the second review found the first build reading straight to paragraph 10h, a base 256 mod 512 that SPEC.md 2.1.1 forbids because dsk_runcap can only split a run at a 64KB page at sector granularity, so a .COM read failed with int 13h error 09h on real hardware whenever the claim's page boundary fell inside the file (~13% of ZEXDOC launches, ~38% of MBASIC's) and QEMU's BIOS never showed it; the harness's read stub now refuses an unaligned base and os88.h states the rule at os88_file_read_seg; the read happens ON THE WAKE (Enter echoes and flushes under W_ONKEY's lock, sets RC_LD_LOAD, and os88_onwake does the floppy work with no lock held - the first build read under the lock, which the review caught: several int 13h calls at ~400 ms each with every other painter stalled). HELLO.COM, the Makefile's hello (49 bytes: nine of code and a 40-byte string - the wave-4 verifier's 'nine bytes' was the code alone), rides the ROOT of build/runcpm.img only, as a build artifact (the loader's launch-folder fallback finds it; the wave-2 gate names it as 'a .COM on build/runcpm.img'), not A\0 and not the 720KB/360KB disks; wave 6's curation decides whether it stays. `tools/getruncpm.py` pins the four artifacts' SHA-256s (`--from DIR` takes a local checkout, `--select KB` chooses A/0 per geometry at recipe time), and the three disks verify (after the first review put the texts and .SUB files ahead of the .COM tail, because the first 360KB disk carried 34 .COM files and no readme, no INFO.TXT, no .SUB and no ZEXDOC; and after the second replaced --select's constant reserve - 48/48/96 clusters, which left ~11KB of package growth on 360/720 while claiming room for 60KB - with a DERIVED budget: the recipe passes --reserve with the root files, priced in the geometry's clusters, and only os88disk.py's own directory arithmetic - folder A, A/0's directory per entry, the one-cluster ASSOC.DAT - stays a constant; it reproduces --verify's 'in use' exactly, so an .OVL beside the package is priced in the day it comes): A0.zip is 79 files, 3 left off, A/0 = 76 + LEFT-OFF.TXT = 77, and LEFT-OFF.TXT names BDOS.SUB and ZCPR3.SUB as the two submit files that will not run, derived from the .SUB contents; 1.44MB 83 files / 1,294 of 2,847 clusters (all 77 + HELLO.COM), 720KB 82 files / 672 of 713 (all 77), 360KB 67 files / 353 of 354 (62: every .COM, .SUB, .TXT and .ME, then MLOAD.DOC, four .LIB and five .Z80 sources). ONE MEASUREMENT THE PLAN DID NOT FORESEE, and it decides where the core keeps its scratch: the first in-OS runs were 5x slower than the raw-QEMU harness for the same core (38 against 180 MHz-equivalent on the banner's own estimate; ZEXDOC heading for ~20 minutes), and neither the wake round trip (8 bursts a wake changed nothing) nor interrupts (cli around the burst changed nothing) were the cause - the per-branch `dec word [cs:_rc_z+RZ_CNT]` was: TCG sends every write to a page that also holds translated code through a slow path, and the package's bss shares its last page with the Sound driver the heap placed just above it (0x9E400, its code run every tick). The budget, the DD/FD pointer and value and the CB operands moved into the Z80's own top 16 bytes (0xFFF0-0xFFFF, above the DPH at 0xFF9F - nothing upstream touches them; a DS-relative word with no override, shorter on the 8088 too), stated in SPEC.md 74. A C package that keeps a hot counter in its bss under QEMU should expect the same, which is worth a LESSONS entry. ONE TOOL DECISION THE PLAN DID NOT FORESEE: `tools/os88disk.py` refused a folder with more than 32 listed entries (the Disk window's cap, a DISPLAY cap - dsk_find and every name-taking cell walk the whole directory); it grew `--deep-folders`, subfolders only, and A\0 holds its 78 files - the alternative was spreading the master disk across user areas, which is not RunCPM's layout. `ESC[u` restores a pending-wrap cursor unclamped and the harness asserts it. Two more from the first review: BDOS 9 advances DE past the '$' as cpm.h's `_RamRead(DE++)` does and C = E is the E of ENTRY (cpm.h sets it before the switch); and the keystroke bound is stated honestly in SPEC.md 74 - one slice plus one flush, the flush at most one scroll + one fill + min(lines, 25) bands, unbounded by the slice's adaptation. The second review's minors, all applied: a scroll rotates the dirty flags with the rows (the flush's refused branch is the one place that dirties every row from top; the flush tracks the drawn cursor's row across the rotation and marks the vacated visible row dirty - both harness-asserted, and a negative control for each fails); a partial expose is ONE fill of the damage rect and the shadow cells KNOWN BLANK, so the text under it and never a band the width of the damage per row; BDOS 9 is bounded to one lap of the 64KB; BIOS MOVE is one rep movsb through the mover with cpm.h's register answers (BC = 0xFFFF); a full key ring rings (input overrun is never silent); HALT sets STATUS_EXIT and takes BOOT's exit path (the "\r\n", the idle) with the toast as the one deviation; os88_onclick kicks so a wake the full event ring refused cannot park a running machine; BDOS 254 is greyed by the structural fact (nothing can sleep inside a slice), not a speed guess. `vm/386-runcpm` exists (`make 386-runcpm`); `.gitignore` covers its nvr/. Files: rcz80.inc, rcmem.inc, rccpm.c, runcpm.c, rcterm.c, rcfs.c, hosttest/{rcuitest.c, os88.h, rcz80test.asm/.sh, rcmemtest.asm/.sh}, tests/rczex.py, tests/rczex_ocr.py, tools/getruncpm.py, tools/os88disk.py, Makefile, vm/386-runcpm/86box.cfg, .gitignore, CLAUDE.md, SPEC.md.

### Wave 3 — BIOS, BDOS console side, the line editor, warm boot and the DRI CCP

- rccpm.c (MIT header): _PatchCPM, _Bios (5/6 no-ops), _Bdos dispatch, console functions 1,2,6,7,8,9,10,11,12 with the retry/state-machine design, 230/231/248-254, mask8bit
- C_READSTR state machine with the cpm.h key map and help text; ^C at an empty line warm-boots
- CCP-DR.60K loaded into a 2KB claim at launch (from the launch folder, before any folder move), copied to 0xE400 on each warm boot; CCPHEAD printed; AUTOEXEC.TXT read once at launch (whole; the first 125 bytes cached) and poked on every warm boot - identical to upstream's read-on-every-boot here, since CP/M cannot reach the launch folder
- key mapping (arrows/Home/End/PgUp/PgDn/Del -> VT sequences, ^C, BkSp=8, Ctrl+- = ^?, Ctrl+Backspace = DEL) and the BEL tone; Alt+F fullscreen chord if Decision 2 says yes
- EXIT (BIOS BOOT) -> exit text -> self-close worker; HALT -> toast -> the same
- Makefile prerequisites re-checked (rccpm.c already listed from wave 1); hosttest/os88.h grows with the tone/key stubs

**Files:** `apps/runcpm/rccpm.c`, `apps/runcpm/runcpm.c`, `apps/runcpm/rcterm.c`, `apps/runcpm/hosttest/rcuitest.c`, `apps/runcpm/hosttest/os88.h`, `Makefile`

**Done when:** 'A>' prompt appears under the CCPHEAD on build/runcpm.img; typing echoes at 2 calls per character; the editor keys behave per cpm.h (^U shows '#', Ctrl+- prints the help, Ctrl+Backspace deletes) - Ctrl-letters verified to arrive as 1..26 in a screendump; DIR/TYPE answer 'NO FILE' (fs stubbed); EXIT closes the window; a keystroke round trip on vm/386-runcpm is not visibly laggy

**Wave 3 — measured (2026-08-17, re-measured after the review):** `os88pkg: 'RUNCPM' image=29504 bss=6933`, 68 functions, largest frame 30 (rc_bdos 18, rc_readstr 16) — +4,656 image / +468 bss over wave 2 for the whole console side, the CCP loop and the exit; 29.5K + 6.9K = 36.4K against the 55,000 trigger, no `RUNCPM.OVL`. **The review's findings, applied:** (1) AUTOEXEC.TXT is read ONCE at launch (`rc_ax_load`, beside the CCP), WHOLE into the Z80 claim's TPA (the loader's 512-aligned landing, ~57KB capacity — `OSAPI_FILE_READ` answers FERR_BIG and reads nothing above the buffer, so the 125-byte read silently ignored any longer file; a 200-byte AUTOEXEC.TXT now runs, `wave3b-autoexec200.png`) and its first 125 bytes cached and poked on every boot — identical to upstream's read-on-every-boot here because CP/M cannot reach the launch folder, and it turns a warm boot into a claim-to-claim copy with NO int 13h (before: a directory walk of the launch folder per ^C/RET/WBOOT, 0.4–2.4 s of UI task on the target, and the file is absent on every shipped disk); (2) `rc_slice` runs on after a warm boot, so CCPHEAD and the CCP's prompt are ONE flush (harness: `^C at the bottom row: warm boot + prompt, one flush` = 5 calls, 1 scroll, 1 fill; was two scrolls); (3) Select is a fact about the DRIVE letter alone as `_sys_select` is: `rc_fs_cd` answers 0 / 1 (drive folder, user folder absent — stands in the drive's folder, 'no file') / −1 (no drive: `Select`), with a per-drive cache beside the per-(drive,user) one, so `USER 1` then `^C` (the DRI CCP restarts with C = 0x10: setuser(1), select(0)) boots to `A>` instead of looping `Bdos Err on A: Select` on every key (`wave3b-user1-ctrlc.png`; harness: the USER 1 script) — wave 4 creates the user folder ON `USER n` as `_SetUser` → `_MakeUserDir` does, not on first write; (4) DRV_SET keeps `cDrive = E` unmasked (`q:` prints `Bdos Err on Q: Select`, `wave3b-select-q.png`), and its error jumps INTO the errwait head in the same trap so a queued key ends the wait at once (harness: typeahead behind `b:`); (5) `os88_onkey` does not flip BLOCKED→RUN while the loader is active, and `rc_ld_back` hands the machine back RUNNING when a key arrived meanwhile; (6) the exit's `\r\n` goes into the model and is not flushed (a scroll destroyed within a tick; harness: 0 calls on the exit wake); (7) F_UPTIME is a 32-bit tick count × 55 (`rc_ticks32` keeps the high word across the kernel's hourly lap; harness: 65,500 then 10 → 0037:0226). BIOS SELDSK's `rc_fs_cd` moving the instance is noted as a stand-in for wave 4's look-up. On the glass (`build/port-shots/wave3-*.png`, VGA): the `A>` prompt under CCPHEAD; `dir` → `no file`, `type x` → `X?`, `foo` → `FOO?` — the DRI CCP's own texts (its `nofmsg` is lowercase, CCP.ASM 711; TYPE of a missing file is `comerr`, not `no file`) against a disk layer answering 0xFF; `abc` `^U` → `abc#` + a fresh line, Ctrl+- → the two help lines and the line retyped, Ctrl+Backspace deletes, `^A ^A ^G` edit in place with the cursor where cpm.h leaves it (zoomed crop) — so Ctrl-letters arrive as 1..26 and Ctrl+-/Ctrl+Backspace as 31/127; `^C` on an empty line → `^C`, CCPHEAD, `A>`; `b:` → `Bdos Err on B: Select`, a key, CCPHEAD, `A>` (DRV_SET through rcfs.c's quiet goto, disk.h's `_error` as a blocked wait); AUTOEXEC.TXT holding `DIR` runs before the first prompt (a scratch image); a disk without CCP-DR.60K prints `Unable to load CP/M CCP.` / `CPU halted.` and idles (stated deviation: upstream exits). **EXIT closes the window through the debug loader** (Alt+L `EXIT`: EXIT.COM from A\0 → BIOS BOOT → `\r\n` → the worker hired on the wake → `os88_wm_destroy` → the dock tile and the window gone, `wave3-after-exit.png`) — a typed `EXIT` needs the CCP to load EXIT.COM, which is wave 4's F_OPEN/F_READ (the wave-3 done_when says 'fs stubbed'). Alt+L is now allowed while the machine is BLOCKED at the prompt (the .COM runs in the CCP's place, RET warm-boots back), which is what keeps `make rczex` working until wave 4 types `ZEXDOC` at the prompt; a refused load / Esc / an empty name go back to the interrupted state (`rc_ldprev`). The harness grew the CCP claim (0x3000), a fake CCP image with the buffer at +6/+7/+8, AUTOEXEC.TXT in the root, the spawn/destroy/alive stubs (alive longjmps), the scripted CCP (13, 14, 2, 2, 10) and the editor script (rows, buffer bytes, A/HL, `^W`, `^X`, `^C`, the Select error's wait, the private calls incl. F_UPTIME's DE:HL, the VT key table, the missing-CCP run); it prices one key into C_READSTR at 1 call / 2 cells and the prompt screen's expose at 12 calls (fill + 11 rows; the wave-2 banner figure was 11). Two things the plan did not foresee: the exit's worker is hired ON the exit (a wake with a short lock hold around `os88_task_spawn`, retried while the task table is full) rather than at the first paint as cword does - so it costs no task switch until then; and `_error`'s "wait for a key" is a state the next trap finds (`rc_errwait`), which finishes the erring function's tail by function number - DRV_SET's in wave 3, wave 4's callers add theirs. The 386 keystroke latency remains the user's reading (86Box could not be driven from the session; the wave-2 note stands). Files: rccpm.c, runcpm.c, rcterm.c (rc_mask, bytes above 0x7F), runcpm.asm (CC_HAS_WORKER), hosttest/os88.h, hosttest/rcuitest.c, SPEC.md.

### Wave 4 — The file system: drives, directory cache, open-file table, records

- rcfs.c (MIT header): place table via os88_file_find over the launch folder and os88_file_goto_q_mark (fallback os88_file_goto); directory cache; F_SFIRST/F_SNEXT with _mockupDirEntry incl. the multi-extent chain; F_OPEN/F_MAKE/F_CLOSE over claims (read_seg/write_seg/regrow), sequential and random reads/writes, F_SIZE/F_RANDREC, ERA/REN, F_ATTRIB = 0, USER folder creation, BDOS 249 MakeDisk, 'Bdos Err on X: Select/R/O', $$$.SUB and _CheckSUB, PUN/LST capture entries, warm-boot flush
- the >64KB and >8-open refusals with their toasts
- rcfstest.c host test incl. 67,301- and 134,899-byte dirents
- full ZEXDOC gate if wave 2 deferred it

**Files:** `apps/runcpm/rcfs.c`, `apps/runcpm/rccpm.c`, `apps/runcpm/hosttest/rcfstest.c`, `apps/runcpm/hosttest/os88.h`, `apps/runcpm/build.sh`, `Makefile`

**Done when:** on build/runcpm.img: DIR lists A\0 (the first DIR after ~N int13 calls, later ones instant); MBASIC loads and runs a typed program and SAVE/LOAD round-trip a .BAS; TE edits and saves a file; PIP copies a file and the FAT12 image opened on the host holds the bytes (walked from the directory entry, not grepped); SUBMIT runs a .SUB; ZEXDOC passes from the A> prompt (make rczex); the size line is under 55,000 resident or the overlay is on

**Wave 4 — measured (2026-08-18, re-measured after the review):** `os88pkg: 'RUNCPM' image=43052 bss=11525`, 120 functions, largest frame 30 — +13,548 image / +4,592 bss over wave 3 for the whole disk layer (`rcfs.c`, ~1,100 lines: 51 functions), 54,577 in all against the 55,000 trigger — 423 bytes under; **the review's SECOND pass (below) took it to 55,053, past the trigger, so the split happened in wave 4, not wave 5: `RUNCPM.OVL` exists — 20 functions moved by FREQUENCY (ERA/REN/MAKE, F_OPEN/F_CLOSE, the search and `_mockupDirEntry`, F_SIZE/F_RANDREC, USER n's folder, BDOS 249, `_CheckSUB`, `_PatchCPM`, the banner's tail, the debug loader; a record, a punched byte, a console byte, the open-file table's loader and the directory cache stay in) — and the wave measures `image=38576 bss=11587` resident (50,163) + `RUNCPM.OVL` 6,155 bytes, 123 functions, 46 resident shims, 17 loading call sites, largest frame 30.** The module's first call is `rc_boot`'s `ovl_patch_cpm` on the first wake, before any folder move; it answers 1, and a 0 (cc_ovneed's toast says why) prints `Unable to load RUNCPM.OVL.` / `CPU halted.` and keeps the window up, as for a missing CCP - so every later `ovl_*` call finds the module in and the BDOS functions keep their natural codes. `--reserve` priced the module into every disk by itself: the 360KB image carries 55 A/0 files (every .COM and .SUB, the texts, MLOAD.DOC, one .LIB, one .Z80) at 354/354, the 1.44MB/720KB all 77 (SPEC.md 74.5). **The second review pass, applied:** (a) every wave-4 refusal goes through one `rc_say` - the toast AND, while fullscreen (where the bar is under the window), the same text on the terminal as `rc_error` prints its line (harness: the `>64K` refusal on the console only while `rc_full`; glass: `wave4c-full-user2.png`); (b) the walk of an area is ANNOUNCED before it starts - `RunCPM: reading A:0`, a toast raised under the gfx lock so it takes SPEC.md 59.4's immediate path (`wave4c-dir-toast.png`), and on the terminal, flushed on the spot, when fullscreen; (c) a `USER n` whose folder the disk refused is remembered for the session (one `os88_file_mkdir`, not one per warm boot; an ERA that frees something lets it be retried - harness); (d) the directory cache is ordered by the HOST name as glob() sorts, compared in host form through a binary search (harness: `A`, `A-B.TXT`, `A.B`, `A.TXT`); (e) ERA of a `PUN.TXT`/`LST.TXT` closes the capture by name from ANY area, as disk.h does (harness); (f) F_READRAND with `r2` set answers 1 with the FCB stepped, as `_sys_readrand`'s successful fseek does (harness); (g) rcfs.c's header carries the measured 238/0, and SPEC.md 73.9 now states the 55,000 trigger it is cited for. **The first review's findings, applied:** (1) the directory cache is TWO slots keyed by (drive,user), least recently wanted replaced (+2KB bss) — a session on B: no longer refills A: and B: on every command, `PIP B:=A:*.*` refills neither (harness: zero walks across `DIR A:`/`DIR B:`/an open on each; glass: `format b:`, `pip b:=a:*.sub`, a `b:` session at 2 `int 13h` calls); (2) `_CheckSUB` — asked at EVERY warm boot — is off the search: a remembered per-user fact kept by MAKE/ERA/REN and by every fill of an (A,u) slot, resolved while unknown by ONE by-name look-up of `$$$.SUB`, so nothing walks a folder on the boot path (measured `DISKCNT=1`: `A>` after 14 `int 13h` calls from the double-click, package and CCP loads included; the harness asserts zero walks of A\0 at the first DRV_ALLRESET and one look-up); (3) the walk is PRICED BY MEASUREMENT, not derivation: 238 `int 13h` calls for A\0's 77 files on a `DIRW1=1` kernel (no sector cache: the quadratic re-seek, ~95 s on the XT, ~560 calls at the 126-file cap) and 0 on the ordinary kernel, where SPEC.md 18.95's track cache serves every re-seek from RAM — the plan's 'first search ~15 s for 37 files' and the header's O(files) claim were both wrong and are replaced by those two numbers in SPEC.md 74.3 and rcfs.c's header; (4) `rc_oft_get` stands in (d,u) before it reads (rc_dc_want does not move when the slot is already valid, and a flush of a B\0 file left the instance there: a punch with A\0's slot valid read B\0's PUN.TXT or lost the byte — harness case added, negative control run); (5) F_CLOSE keeps the entry clean instead of dropping it, so MBASIC's SAVE then LOAD, TE's save-and-continue and PIP's passes read nothing (harness: zero read_seg calls on the reopen) — but the warm boot still RELEASES every entry, deliberately: RunCPM does not hold heap for programs that are over (a CP/M machine's CCP reads a .COM from the disk every time), the one part of that finding not taken, stated in SPEC.md 74.3; (6) `REN` takes the row out and inserts it back in name order (the harness checks the order AFTER the rename now) and the held bytes follow the new name; (7) a sequential write past record 510 or a record the heap will not hold answers 0xFF, upstream's failed-`fwrite` code, not 2 — SPEC.md 74.3 says so beside the 6; (8) the plan's toast text and rc_bdos's stale header sentence fixed. (the size-per-line came out at ~12.5 bytes: SmallerC's struct indexing and the 16-bit-safe arithmetic - `(size >> 10) + ((size & 1023) ? 1 : 0)`, never `(size + 1023) >> 10`, which wraps at 64KB - are bulkier than the plan's 5.9 figure). **The done_when, on the glass** (`build/port-shots/wave4-*.png`, VGA, `build/runcpm.img` copied to a scratch image so the writes are inspectable): DIR lists A\0 (sorted, as upstream's glob is - `wave4-dir.png`) and a second DIR is served from the cache (harness: zero `os88_file_find` calls; the first fill of A\0 is MEASURED above with `DISKCNT=1` - 238 `int 13h` calls without the sector cache, 0 with it - and paid at the first DIR, never at boot); MBASIC runs a typed program and `SAVE "T"`/`NEW`/`LOAD "T"`/`LIST`/`RUN` round-trip (`wave4-mbasic.png`); TE edits and `save As` a file which `TYPE` then prints (`wave4-te-type.png`); `pip copy.txt=hello.txt` (`wave4-pip.png`) and the FAT12 image WALKED on the host - root → `A` → `0` → `COPY.TXT`'s cluster - holds HELLO.TXT's 128 bytes exactly; `submit clean` runs its five ERAs in order and `submit info` assembles INFO.COM with Z80ASM (`wave4-submit-*.png`; `$$$.SUB` gone from the directory after), and the new INFO.COM prints `running on Unknown` (`wave4-info.png`); `user 1` / `save 1 u1.bin` makes `A\1\U1.BIN` (walked); `era copy2.txt` removes it (walked); ZEXDOC typed at the prompt passes (`make rczex`, which now types the name at `A>` - `--loader` keeps Alt+L). **The harness** `hosttest/rcfstest.c` (`build.sh` runs it) drives `rc_bdos` over a fake folder tree with contents and a regrow that ALWAYS MOVES: names (the seven `_FCBtoHostname` rules), `match()`, the directory-entry chain for 0/127/128/16384/16385/32768/65535/67301/134899 bytes (by hand from disk.h 229-267: 67,301 = `ex` 1/3/4 `rc` 128/128/14, 134,899 = five entries ending `ex` 8 `rc` 30), open/read/write/close with the partial record's `^Z`, random records with the zero gap and the FCB positions, the extent roll at 128 records, `F_MAKE` keeping an existing file's bytes, the eight-and-ninth table (eviction, the dirty ninth's `CP/M ERR` + toast, the key ending the wait), $$$.SUB as SUBMIT and the DRI CCP use it (rc/cr, `s2 = 0; rc--; close` truncating, delete at 0), USER 3 making its folder and the all-users search, a full disk's USER, ERA with a pattern (a made-unwritten file too), REN (an open modified file flushed first), PUN/LST capture across flushes and after ERA, Select and R/O errors from disk functions, BDOS 249, the warm-boot flush, no-memory eviction. **Two things the plan did not foresee, both found on the glass:** (1) `Not saved: HELLO.TXT` on the SECOND save of a session - THE KERNEL DOES NOT GROW A DIRECTORY (SPEC.md 18.5: FERR_DIRFULL at the end of the chain) and `os88disk.py` sized A\0's directory to its 77 files plus one spare slot; fixed on the disk, not in the kernel (204 bytes left in its image rung, and it is an OS-wide fact - every folder the OS makes is one cluster): `os88disk.py --dir-slots FOLDER=N` and `getruncpm.py --select --dir-slots N` (`RUNCPMSLOTS := 128`, the cache's size: A\0 holds 126 files on every geometry) - a USER n or BDOS 249 folder is one cluster = 14 files on a 1.44MB disk, stated in SPEC.md 74.3, and the write-back toast names the fact (`Dir full:` / `Disk full:` / `Protected:` / `Not saved:`); the 360KB disk ships FULL (354/354 clusters), so no session can save on it until wave 6's curation leaves room - flagged for that wave. (2) The directory cache is SORTED (insertion at fill, sorted insert on MAKE): upstream's `glob()` sorts and DIR read alike; the first build listed FAT order. **Two defects the harness found before the glass:** the fill used the caller's name scratch (`rc_n11`), so an F_OPEN that triggered a refill opened the LAST file walked instead of the one asked - a static-buffer aliasing of exactly the kind SPEC.md 73.5's `static` idiom invites (its own scratch now, `rc_dc_n11`, and the test refills before an open); and ERA of a made-and-not-yet-written file errored R/O (upstream's fopen "a" creates the host file at MAKE; here NOENT on the delete counts as deleted). `rcmem.inc` grew `rc_sfill(seg, off, v, n)` (a claim's zero gap), tested in `rcmemtest.asm` beside the others. Files: rcfs.c, rccpm.c, runcpm.c, rcmem.inc, hosttest/{rcfstest.c (new), os88.h, rcuitest.c, rcmemtest.asm}, build.sh, tools/os88disk.py, tools/getruncpm.py, tests/rczex.py, Makefile, SPEC.md.

### Wave 5 — About, greying facts, polish of the console

- rcabout.c About panel (<= 12 rows) with MIT attribution (ovl_ if split); shadow invalidation after any panel/menu/fullscreen change
- greyed facts wired: 254 no-op, 64KB refusal toast, open-file cap toast, unknown escapes swallowed, mono attribute rule, INFO.COM 'Unknown' stated
- cost pass on the terminal: coalesced scrolls (the pacing wave 2 chose, re-measured with real programs); the band/font_run choice and the same-row split are already measured (Set 65) and are re-checked only if the numbers move
- full VIDEO=cga and Hercules (hercshot) look at the prompt, a DIR, TE's 25-row screen (framed and fullscreen) and the About box

**Files:** `apps/runcpm/rcabout.c`, `apps/runcpm/rcterm.c`, `apps/runcpm/runcpm.c`, `apps/runcpm/hosttest/rcuitest.c`, `apps/runcpm/hosttest/os88.h`, `Makefile`

**Done when:** About shows on VGA and CGA with OK inside the panel; the harness cost table still shows what §74.2's measured table states (echo 1 call / 2 cells, a scrolled DIR row 4 calls / 61 cells, ESC[2J 1 fill + the cursor, TE full redraw 25 bands, Alt+F then 0) or the SPEC and the harness are changed together; a 40KB TYPE scrolls without a full repaint anywhere (counter in the harness = 1 scroll per line); TE's status line is on the glass on VGA/Hercules and on CGA fullscreen

**Wave 5 — measured (2026-08-18):** `os88pkg: 'RUNCPM' image=39356 bss=11689` resident (51,045) + `RUNCPM.OVL` 7,389 bytes, 128 functions (23 in the module), 55 resident shims, 20 loading call sites, largest frame 30 — +780 image / +102 bss / +1,234 module over wave 4, under the 55,000 trigger. **The About panel** (`rcabout.c`, SPEC.md 74.4): 12 rows (the 640x200 number - `6 + 10*10 + 10 = 116` against ~122; the harness asserts OK inside the panel and the panel inside the content box on 80x25, 79x25 AND 78x17), the strings from `readme.md`'s title, `main.c` 76's banner line and `LICENSE`'s copyright line plus three lines saying what the port is, drawn as one fill + two frames + a band per label (`rc_draw_band`, x snapped to a byte boundary) + the OK button = 26 calls / 266 cells; **the machine is PAUSED while it is up** (no slice, no wake, no flush - stated in 74.4 as the one behaviour the panel adds; keys are swallowed except Esc/Enter/space, Alt+F is refused, a click closes only on OK), and **the close is the panel's rectangle as damage** through `rc_blank_rect` (factored out of `os88_paint`'s partial-expose path so the two cannot drift): over a full TE screen 1 fill + a band per row the panel covered (20 calls / 977 cells for 17 rows) against the 26 calls / 1,903 cells a shadow invalidation would have cost. The show is `ovl_*`; the close and the hit test (a dozen compares) resident. **The greyed facts, re-checked as wired:** BDOS 254 accepted and ignored (`rccpm.c`), the `>64K` and `8 files open` toasts through `rc_say` (`rcfs.c`), escapes outside the subset swallowed and only SGR 7 rendered (`rcterm.c`), INFO.COM's `Unknown` stated in 74.4 - nothing new to wire; the About box carries none of them (LESSONS 8). **The cost pass:** the harness grew a 40KB TYPE (512 lines of 78 characters, three lines a slice - the target's pacing): 163 scrolls, 163 fills, 683 bands, 1,009 calls in 171 flushes - two calls a line, no full-content fill anywhere, scrolls == flushes once the screen is full - and the row is asserted; on the glass `TYPE Z80CCP.ASM` (1,569 lines, 37KB) read off the new **Alt+C** counters toast (`gfx <calls>/<cells> scr <n>`, the plan's Verification instrument, module code) is 866 calls / 35,662 cells / 3 scrolls under QEMU, whose slices put more than a screen out at once (a scroll of 25+ rows is the row redraw, cheaper); the wave-1/2 rows of the table are unchanged (echo 1/2, scrolled DIR row 4/61, ESC[2J 2/1, TE redraw 25, Alt+F then 0). **The three adapters** (`build/port-shots/wave5-*.png`): VGA - launch, DIR, About (and its OK at zoom 6), About over TE and closed, TE framed (79 columns: `Len:4`) and fullscreen (`Len:47`, 25 rows); CGA (`--screen 640x200`) - launch, DIR, About with OK inside the panel and the panel inside the window, TE framed (17 rows) and fullscreen (80x25 with `ESC = menu` on row 24); Hercules (`hercshot`) - launch, DIR, About over TE and closed by Enter, TE framed and fullscreen. **One thing the glass showed that the plan had claimed measured:** Hercules' 720 px did NOT show 80 columns framed - the window was 640 wide on every adapter, so its bordered content was 638 px there (`Len:4`); the window is now authored `RC_COLS*8 + 2 = 642` wide, which `wm_fit` clamps to 640 (flush, borderless, 79 columns) on a 640-px screen exactly as before and leaves at 642 with its border on Hercules, where 640 px of content hold all 80 (`Len:47` framed, `wave5-te-herc.png`); SPEC.md 74.2 says so. **Wave 4's two carry-overs:** (a) the column-0 glyph loses no pixel - the wave-4 shot's own pixel data (`wave4-verify-dir-toast.png` rows 64-68) has x = 0 black for the IBM 8x8 'A' (`30 78 CC CC FC CC CC`), and `wave5-dir-vga-col0-zoom.png` at zoom 8 has both legs 16 px wide; what the verifier saw is the image VIEWER eating the leftmost pixel column of a 640-px image at the screen's edge, not the package's origin nor the window system's frame; (b) HELLO.COM is 49 bytes (nine of Z80 and a 40-byte string) - the Makefile comment, SPEC.md 74.5 and the wave-2 paragraph now say so. `make rczex` re-run on the wave-5 build: 67 of 67 OK in 565 s (a loaded host: wave 4's runs were 146-211 s; the slice path gained one compare, `rc_abt`, per wake). Files: rcabout.c, rcterm.c, runcpm.c, hosttest/{rcuitest.c, os88.h}, Makefile (a comment), SPEC.md.

### Wave 6 — Icon, disk curation, docs, licence re-check

- icon.inc (16 rows mask + 16 data); 360KB curation (.COM/.SUB/.TXT/.ME) and the 720KB/1.44MB manifest of what fits, listed; 1STREAD.ME + a note naming the three files left off; RUNCPM\ folder on apps-all.img with the CCP beside it (Decision 3)
- XT measurement recorded; xt-runcpm only if the measured speed justifies it
- README/SPEC numbers re-measured at the end (image, bss, ovl, calls, MHz per machine); the MIT header re-check across every file, the About box and the PR body; 1STREAD.ME's per-program credits on the disk

**Files:** `apps/runcpm/icon.inc`, `tools/getruncpm.py`, `Makefile`, `SPEC.md`, `README.md`, `apps/runcpm/*.c headers`

**Done when:** make runcpmdisk produces runcpm.img/720/360 that verify with no file over 65,535 bytes; make allapps carries RUNCPM\ and it boots to A> from that disk; make 386-runcpm boots to the A> prompt on 86Box and the banner's MHz is in SPEC; checkdocs passes

**Wave 6 — measured (2026-08-18):** `os88pkg: 'RUNCPM' entry=+0x0060 image=39412 bss=11689 icon=yes` resident (51,101) + `RUNCPM.OVL` 7,389 bytes, 128 functions (23 in the module), 55 resident shims, 20 loading call sites, largest frame 30 — +64 bytes over wave 5 for the icon block, −8 for the About line the review shortened ("A native reimplementation of RunCPM 6.9 for": the core is 8086 assembly, so "in C" was half true), and no other C changed. **The icon** (`apps/runcpm/icon.inc`, `CC_ICON` in the shim, a `.bin` prerequisite in the Makefile): RunCPM's own, `RunCPM/RunCPM.ico` (IDI_ICON1 in `RunCPM.rc`; one 16x16 8-bit image), transcribed pixel for pixel - a two-row window strip with a white pixel at its left and a red close box at its right, over a dark field with a large green `A>` (the A seven wide, the > five, rows 4-13) - and read at 1bpp: field and strip black, glyph and close box white, the mask all sixteen rows full, ONE stated deviation (row 2 white where the .ico is dark, so the strip does not vanish into the field on a 1bpp screen). The first draft was a monitor on a stand drawn from imagination, with a 3x5 `A>`; the review found the .ico, which the authority table had no row for, and LESSONS 1 decided it. The transcription is checked programmatically against the .ico's pixels (rows 4-13's green -> the 16 data words); looked at in the Disk window's list and icon views, in the dock and on CGA at zoom 8 (`build/port-shots/wave6-icon-zoom.png`, `wave6-icon-grid-zoom.png`, `wave6-dock-zoom.png`, `wave6-cga-icon-zoom.png`). **The curation** (`tools/getruncpm.py --select`): the 360KB disk carries the programs and the texts - every .COM, .SUB, .TXT and .ME, 52 files - and none of the sources, libraries or documentation (`CURATED = {360: [.TXT, .ME, .SUB, .COM]}`), so it ships at 350/354 clusters with 4KB free where wave 4's shipped full; the 720KB and 1.44MB disks carry all 77 (`ABDOS.60K`/`.64K` were dropping out of the first curated build because their extensions are outside the category list - the list ranks, it does not filter, on the uncurated geometries); and **each geometry's `LEFT-OFF.TXT` is its own** (`build/runcpm-disk/left-off/<kb>/`, printed by --select in place of A/0's and priced at its real size, re-selecting until it fits its allowance): the three files above 65,535 bytes, every .SUB whose source this disk lacks (derived from the .SUB contents against everything off the disk - two on 720KB/1.44MB, eleven of twelve on 360KB, only CLEAN.SUB runs there; the review caught the first draft naming only the size-limit two), what this geometry leaves off by policy and, under its own heading, for want of room, and that `1STREAD.ME` (upstream's text, on A/0 and in the root, unedited) credits the programs beyond Digital Research's distribution - it does not credit every program, and the note no longer says it does. HELLO.COM - this tree's 49-byte hello, wave 2's first proof - is off the shipping 1.44MB image since the review (a released disk carries nothing invented here, in the root as much as in A/0); `build/HELLO.COM` is still built for a scratch-image test of Alt+L's launch-folder path, and `tests/rczex.py` finds `RUNCPM.O88` at the root's fifth listed row without it (re-run twice: 67 of 67 at 557 s and 515 s on a host whose raw `rcz80test` took 179 s the same hour and whose previous package - the 39,420-byte one, re-built and re-run as a control - took 513 s: the host's spread, not the code's). **`apps-all.img`** carries `RUNCPM\` (the package, the .OVL, the CCP, LICENSE, 1STREAD.ME, `A\0` = the 1.44MB selection through the same shell substitution and empty-selection guard as RUNCPMIMG, `--deep-folders --dir-slots RUNCPM/A/0=128`; `--reserve` prices the disk's own files, `ALLAPPSFILES`, and `--folders $(ALLAPPSFOLDERS)` its other folder directories - counted by make from the disk's own arguments, every DIR: prefix, its parent, DOCS and RUNCPM\A, ten today, after the review found the constant - so the budget is derived here as it is for `runcpm.img`; the first draft reserved the prerequisite list), `allapps` depends on `$(RUNCPMDEPS)` and so on the fetch stamp: 112 files in 11 folders, 2,212 of 2,847 clusters, and it boots to `A>` from `RUNCPM\` with `DIR *.COM` listing the 37 (`wave6-all-a-prompt.png`). **On the glass:** the 360KB image boots to `A>`, `DIR` lists its 52 (`wave6-360-dir.png`), `pip test.txt=info.txt` then `type test.txt` save and read back on it (`wave6-360-pip.png`), `type left-off.txt` prints the note (`wave6-360-leftoff.png`; its .SUB block, all eleven, in TE at `wave6-360-leftoff-te.png` - TE.COM will not open the dashed name - `Can't open` - while TYPE and PIP open it through the same F_OPEN, so the refusal is upstream of the BDOS call in a binary that was not read, and PIP's copy `LEFTOFF.TXT` is what it shows); `VIDEO=cga` (`--screen 640x200`): the About panel with OK inside it (`wave6-cga-about.png`), `b:` -> `Bdos Err on B: Select` and INFO.COM's `running on Unknown` - the two refusals a CGA user meets first (`wave6-cga-info.png`). **The documents:** SPEC.md 74 (the shipping size line, 74.3's 360KB sentence, 74.5 rewritten for the curation, the per-geometry note, HELLO.COM's decision and apps-all's counts, the 86Box readings' recipe), README.md's paragraph beside CWORD's and the image table's row, CLAUDE.md's command block (`make runcpm`/`runcpmdisk`/`rczex`/`rcz80test`/`rcmemtest`, allapps' new prerequisite), LESSONS.md 13, and the MIT attribution added to the three harness files and the gate that carry RunCPM's expected texts (hosttest/rcfstest.c, rcuitest.c, rcz80test.asm, tests/rczex.py) - every package source already carried it. **Still pending, deliberately:** the 386 and XT clock readings (86Box cannot be driven from a session; SPEC.md 74 says how they are taken and where they go), and with them the 8086 slice length; no `xt-runcpm` target until the XT figure exists.

## What shipped

RunCPM 6.9 as `RUNCPM.O88` (39,412 image + 11,689 bss = 51,101 resident of 61,440) + `RUNCPM.OVL` (7,389), 128 C functions in one translation unit (`runcpm.c` + `rcterm.c`, `rccpm.c`, `rcfs.c`, `rcabout.c`) and three hand-written includes (`rcz80.inc` ~1,900 lines - the Z80, `rcmem.inc`, `rcband.inc`) plus the icon; a kernel that grew a wake event and an instance-marking quiet goto (+181 `.text` / +44 `.bss`, three API slots, no budget moved); ZEXDOC 67 of 67 groups from the `A>` prompt; three floppies and a folder on `apps-all.img`; a host harness (`rcuitest.c`, `rcfstest.c`) and two boot-sector harnesses (`rcz80test`, `rcmemtest`) that `make runcpm` runs first; `vm/386-runcpm`. Everything in the plan's *Ships* list is on the glass except the two 86Box clock readings, which are the user's; everything in *Present and greyed* is present and states its fact; everything in *Absent* is absent. The waves' measured paragraphs above are the record of what each cost and what each review found; SPEC.md 74 is the contract; `.claude/skills/port-to-os8088/LESSONS.md` 13 is what the next port should read first.

## Verification

- Bench harness tests/rcband/rcbandbench.asm (`make rcbandbench`, then `make test TESTAPPS=build/rcband.img QEMU="qemu-system-i386 -icount shift=3,sleep=off"`): the package's own rcband.inc timed against the 79-cell FONT_RUN in one run, with the first loop kept for the record and identity rows for correctness; PERFORMANCE.md Set 65 is its table
- Host harness apps/runcpm/hosttest/rcuitest.c (clang, run first by apps/runcpm/build.sh and the make rule): stub os88.h + glass model (cellch/pixline, blit1 and scroll MODELLED not refused, scroll vacated rows filled with garbage), feed byte streams and escape sequences, assert model == glass and shadow == glass after every step, print the cost table in calls/cells/us for: echo one char, one DIR row, one scrolled line, ESC[2J, cursor-only move, TE-style 25-row redraw, full expose repaint
- Host test rcfstest.c: FCB/name/wildcard/dirent/record math against a fake folder incl. 67,301- and 134,899-byte files (chain listed, open refused), OFT eviction, $$$.SUB
- Boot-sector harness rcmemtest (QEMU) for the ES-loading movers with SS != DS and negative controls
- LESSONS 2 stub build before wave 2: dispatch tables + stub handlers + stubbed C -> os88pkg size line, recorded in SPEC with the resident/overlay decision
- make rczex (tests/rczex.py): QEMU boots build/runcpm.img, launches RUNCPM by the two-press double-click driver, sends 'ZEXDOC' + Enter via tools/qmp.py sendkey (or the debug loader key in wave 2), polls tools/shot.py crops of the terminal rows through tests/rczex_ocr.py until 'Tests complete', fails on any 'ERROR' line; runtime measured once and written into the recipe; ZEXALL on demand overnight
- QMP recipe: tools/qmp.py build/qmp.sock quit; rm -f build/qmp.sock; make test TESTAPPS=build/runcpm.img; double-click Disk B then RUNCPM; sendkey the CP/M command; tools/shot.py --crop <terminal rows> --zoom 4 before believing anything; repeat with VIDEO=cga (--screen 640x200) and VIDEO=herc HERCSEG=0x7000 + tools/hercshot.py; save into a scratch copy of the image and verify a saved file by walking its FAT12 directory entry
- Wave 1 wake/goto proof: a screendump of the wake counter climbing with no key; a read after goto_q_mark of a file that exists only in the target folder
- 86Box: vm/386-runcpm (386, B: = build/runcpm.img) - read the banner's 'Estimated Z80 clock speed', time a DIR of 37 files first and second, time TYPE of a 20KB file, watch a TE full-screen redraw for flash; then the same 360KB disk on vm/xt640 (640KB, 4.77 MHz 8088; fdd_02_fn hand-pointed for the session, cfg git-checkout'ed after) to record the XT figure before any xt-runcpm target exists
- Counters: a counter in the terminal flush (calls, cells, scrolls) toasted on demand by a debug key during development, multiplied by the table (756us/call, 900us/cell); nothing repaints more than it changed - a full repaint per keystroke or per line is a defect
- Size: os88pkg's line tracked from the first commit; frame report on every build; 55,000 resident is the split trigger; kernsize before/after the wave-1 kernel change quoted against 429/1,024

## Risks

- The wake mechanism and the goto_q_mark variant are a kernel change: kernsize[big] today shows the image rung 57,344 with 429 bytes left and the footprint 1,024 spare (2 steps) - ~100-150 bytes fit in .text without a rung crossing, but it is measured, not assumed, in wave 1; if refused (Decision 1) the design falls back to a Frotz-style worker that draws under short lock holds and a RAM-drive with write-back only at UI-task moments, which loses unsaved files on the close box
- The event queue is 16 records and drops silently when full: a dropped wake stalls the emulator until the next paint/key/click kicks it - the design re-posts from every callback and never posts twice; verify by flooding clicks while ZEXDOC runs
- Z80 speed on the XT is NOT yet a number: the draft's 0.25-0.35 MHz rested on an inner loop that could not be written (lodsb clobbers A; no free base register); the corrected fetch is 5 instructions plus a cs: prefix and (HL) accesses through DI are free but stack ops through BP pay a ds: prefix. The first core build measures it on vm/xt640 and vm/386-runcpm and the banner and SPEC state what it means (MBASIC/TE usable? Z80ASM minutes? ZEXALL not for the XT?)
- Budget is at the trigger before a line is written (36-47KB image + 11.5KB bss at cword's all-in 5.9 bytes/line); the stub build before wave 2 decides the split, and if an overlay exists it must be loaded before the first folder move because the .OVL resolves in the instance's folder
- Fullscreen (Decision 2) deviates from SPEC 11.2.1's F/Esc binding by necessity - a terminal owns both keys; the Alt+F chord must be verified to arrive with ascii 0 and a distinct scan code through the BIOS keyboard path, and it must be checked that Alt+letter is not consumed by the kernel first
- First DIR/first open of a folder is O(files) int 13h calls (~400 ms each on the XT: 37 files ~15 s once) because os88_file_find re-walks by ordinal; with the os88_file_goto fallback every (drive,user) switch is a full dsk_chdir (listing, sort, icon harvest) on top of that - the directory cache makes later searches free, but the first one must be seen on hardware and stated
- Whole-file model: F_CLOSE writes the entire file (1-3 s on the XT for a 20KB file); random-access programs that keep files open and never close lose changes only if the machine dies before warm boot - the same as CP/M with a dirty buffer, but say so
- Typing latency is a full round trip (key -> ring -> Z80 program echo -> terminal): with 50 ms slices it is under 100 ms on the XT; a slice that is too long makes the machine feel dead - size N by os88_cpu() and measure
- The Z80 F byte in lahf layout: N and P/V(overflow) need per-op fixups and X/Y undocumented bits are ignored - ZEXDOC (documented flags) is the gate; ZEXALL (undocumented) may show differences we accept and state; ZEXDOC's own runtime in QEMU is unmeasured and may push the full gate to wave 4
- No printf: every banner/estimate/hex string is built with utoa/strcpy; every out-parameter and buffer static; the C_READSTR recall buffer static not calloc
- Keyboard: Ctrl-H/I/M fold into BS/Tab/CR (identical bytes, no loss); Ctrl+Space arrives as 32; Ctrl-letters must be verified to arrive as 1..26, and Ctrl+- (^?) and Ctrl+Backspace (DEL) must be verified to arrive at all, in a wave-3 screendump
- MBASIC/Z80ASM/others on A0.zip are not MIT-licensed; shipping them is the user's call (Decision 3); the tool fetches at build so nothing third-party is committed except, if chosen, the 2KB DRI CCP
- Only one hand in the translation unit at a time; make cannot see through #include - every .c and .inc is a written prerequisite from wave 1, and stubs exist from wave 1 so no later wave adds a file the Makefile does not name
- The 720KB/1.44MB disks cannot carry all of A0.zip (875,602 bytes over 79 files) beside the package - measured in wave 2: with the three files above 65,535 bytes left off (606,898 bytes remain) BOTH carry all of it, and only the 360KB disk chooses; the manifest is curated in wave 6 and what is left off is listed
